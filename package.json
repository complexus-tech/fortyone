{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:caddy": "turbo run dev",
    "dev": "concurrently \"turbo run dev\" \"caddy run\" --names \"turbo,caddy\" --prefix-colors \"blue,green\"",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "tsconfig": "workspace:*",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@9.3.0",
  "name": "fortyone.tech",
  "pnpm": {
    "overrides": {
      "@types/react": "19.1.9",
      "@types/react-dom": "19.1.7"
    }
  }
}
