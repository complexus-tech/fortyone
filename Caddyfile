# Main landing app (fortyone.lc)
fortyone.lc {
	tls internal
	reverse_proxy localhost:3000
}

complexus.fortyone.lc {
	tls internal
	reverse_proxy localhost:3000
}

# Docs app (docs.fortyone.lc)
docs.fortyone.lc {
	tls internal
	reverse_proxy localhost:3002
}

# Optional: redirect www to main domain
www.fortyone.lc {
	tls internal
	redir https://fortyone.lc{uri}
}


