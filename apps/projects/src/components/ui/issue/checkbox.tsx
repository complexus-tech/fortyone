import { Checkbox } from "ui";

export const IssueCheckbox = () => {
  return (
    <Checkbox className="absolute -left-[1.6rem] hidden group-hover:inline-block data-[state=checked]:inline-block" />
  );
};
