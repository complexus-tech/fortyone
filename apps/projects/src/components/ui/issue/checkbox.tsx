import { Checkbox } from 'ui';

export const IssueCheckbox = () => {
  return (
    <Checkbox className='absolute -left-[1.6rem] hidden data-[state=checked]:inline-block group-hover:inline-block' />
  );
};
