// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: push.sql

package notificationssql

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const disableNotificationPushDevices = `-- name: DisableNotificationPushDevices :execrows
UPDATE public.notification_push_devices
SET disabled_at = CAST($1 AS timestamptz),
    updated_at = CAST($1 AS timestamptz)
WHERE expo_push_token = ANY(CAST($2 AS text[]))
  AND disabled_at IS NULL
`

type DisableNotificationPushDevicesParams struct {
	DisabledAt     time.Time
	ExpoPushTokens []string
}

func (q *Queries) DisableNotificationPushDevices(ctx context.Context, arg DisableNotificationPushDevicesParams) (int64, error) {
	result, err := q.db.Exec(ctx, disableNotificationPushDevices, arg.DisabledAt, arg.ExpoPushTokens)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getNotificationPushDelivery = `-- name: GetNotificationPushDelivery :many
WITH notification_scope AS (
    SELECT
        notification.notification_id,
        notification.recipient_id,
        notification.workspace_id,
        notification.entity_type,
        notification.entity_id,
        notification.title,
        notification.message,
        workspace.slug AS workspace_slug,
        membership.role
    FROM public.notifications AS notification
    INNER JOIN public.users AS recipient
        ON recipient.user_id = notification.recipient_id
       AND recipient.is_active = TRUE
       AND recipient.is_system = FALSE
    INNER JOIN public.workspaces AS workspace
        ON workspace.workspace_id = notification.workspace_id
       AND workspace.deleted_at IS NULL
    INNER JOIN public.workspace_members AS membership
        ON membership.workspace_id = notification.workspace_id
       AND membership.user_id = notification.recipient_id
       AND membership.role IN ('admin', 'member', 'guest')
    LEFT JOIN public.notification_preferences AS preference
        ON preference.user_id = notification.recipient_id
       AND preference.workspace_id = notification.workspace_id
    WHERE notification.notification_id = CAST($1 AS uuid)
      AND notification.in_app_enabled = TRUE
      AND notification.push_sent_at IS NULL
      AND CAST(notification.entity_type AS text) <> 'feedback'
      AND COALESCE(
          CAST(
              preference.preferences
                  -> CAST(notification.type AS text)
                  ->> 'push'
              AS boolean
          ),
          TRUE
      ) = TRUE
), visible_notification AS (
    SELECT scope.notification_id, scope.recipient_id, scope.workspace_id, scope.entity_type, scope.entity_id, scope.title, scope.message, scope.workspace_slug, scope.role
    FROM notification_scope AS scope
    WHERE
        (
            CAST(scope.entity_type AS text) = 'story'
            AND EXISTS (
                SELECT 1
                FROM public.stories AS story
                WHERE story.id = scope.entity_id
                  AND story.workspace_id = scope.workspace_id
                  AND story.deleted_at IS NULL
                  AND (
                      scope.role = 'admin'
                      OR EXISTS (
                          SELECT 1
                          FROM public.team_members AS team_membership
                          WHERE team_membership.team_id = story.team_id
                            AND team_membership.user_id = scope.recipient_id
                      )
                  )
            )
        )
        OR (
            CAST(scope.entity_type AS text) = 'comment'
            AND EXISTS (
                SELECT 1
                FROM public.story_comments AS comment
                INNER JOIN public.stories AS story
                    ON story.id = comment.story_id
                   AND story.workspace_id = scope.workspace_id
                   AND story.deleted_at IS NULL
                WHERE comment.comment_id = scope.entity_id
                  AND (
                      scope.role = 'admin'
                      OR EXISTS (
                          SELECT 1
                          FROM public.team_members AS team_membership
                          WHERE team_membership.team_id = story.team_id
                            AND team_membership.user_id = scope.recipient_id
                      )
                  )
            )
        )
        OR (
            CAST(scope.entity_type AS text) = 'objective'
            AND EXISTS (
                SELECT 1
                FROM public.objectives AS objective
                WHERE objective.objective_id = scope.entity_id
                  AND objective.workspace_id = scope.workspace_id
                  AND (
                      scope.role = 'admin'
                      OR EXISTS (
                          SELECT 1
                          FROM public.team_members AS team_membership
                          WHERE team_membership.team_id = objective.team_id
                            AND team_membership.user_id = scope.recipient_id
                      )
                  )
            )
        )
        OR (
            CAST(scope.entity_type AS text) = 'key_result'
            AND EXISTS (
                SELECT 1
                FROM public.key_results AS key_result
                INNER JOIN public.objectives AS objective
                    ON objective.objective_id = key_result.objective_id
                   AND objective.workspace_id = scope.workspace_id
                WHERE key_result.id = scope.entity_id
                  AND (
                      scope.role = 'admin'
                      OR EXISTS (
                          SELECT 1
                          FROM public.team_members AS team_membership
                          WHERE team_membership.team_id = objective.team_id
                            AND team_membership.user_id = scope.recipient_id
                      )
                  )
            )
        )
        OR (
            CAST(scope.entity_type AS text) = 'strategy'
            AND (
                scope.role = 'admin'
                OR scope.message -> 'strategy' ->> 'kind' = 'weekly_check_in'
            )
        )
)
SELECT
    visible.notification_id,
    visible.recipient_id,
    visible.workspace_id,
    visible.entity_type,
    visible.entity_id,
    visible.title,
    visible.message,
    visible.workspace_slug,
    COALESCE(device.expo_push_token, '') AS expo_push_token
FROM visible_notification AS visible
LEFT JOIN public.notification_push_devices AS device
    ON device.user_id = visible.recipient_id
   AND device.disabled_at IS NULL
ORDER BY device.device_id NULLS LAST
`

type GetNotificationPushDeliveryParams struct {
	NotificationID uuid.UUID
}

type GetNotificationPushDeliveryRow struct {
	NotificationID uuid.UUID
	RecipientID    uuid.UUID
	WorkspaceID    uuid.UUID
	EntityType     EntityType
	EntityID       uuid.UUID
	Title          string
	Message        []byte
	WorkspaceSlug  string
	ExpoPushToken  string
}

// GetNotificationPushDelivery repeats the inbox visibility boundary so a
// queued delivery cannot reveal content after workspace or team access changes.
func (q *Queries) GetNotificationPushDelivery(ctx context.Context, arg GetNotificationPushDeliveryParams) ([]GetNotificationPushDeliveryRow, error) {
	rows, err := q.db.Query(ctx, getNotificationPushDelivery, arg.NotificationID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetNotificationPushDeliveryRow{}
	for rows.Next() {
		var i GetNotificationPushDeliveryRow
		if err := rows.Scan(
			&i.NotificationID,
			&i.RecipientID,
			&i.WorkspaceID,
			&i.EntityType,
			&i.EntityID,
			&i.Title,
			&i.Message,
			&i.WorkspaceSlug,
			&i.ExpoPushToken,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listNotificationPushTokensForUser = `-- name: ListNotificationPushTokensForUser :many
SELECT device.expo_push_token
FROM public.notification_push_devices AS device
INNER JOIN public.users AS recipient
    ON recipient.user_id = device.user_id
   AND recipient.is_active = TRUE
   AND recipient.is_system = FALSE
WHERE device.user_id = CAST($1 AS uuid)
  AND device.disabled_at IS NULL
ORDER BY device.device_id
`

type ListNotificationPushTokensForUserParams struct {
	UserID uuid.UUID
}

func (q *Queries) ListNotificationPushTokensForUser(ctx context.Context, arg ListNotificationPushTokensForUserParams) ([]string, error) {
	rows, err := q.db.Query(ctx, listNotificationPushTokensForUser, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var expo_push_token string
		if err := rows.Scan(&expo_push_token); err != nil {
			return nil, err
		}
		items = append(items, expo_push_token)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markNotificationPushSent = `-- name: MarkNotificationPushSent :execrows
UPDATE public.notifications
SET push_sent_at = CAST($1 AS timestamptz)
WHERE notification_id = CAST($2 AS uuid)
  AND push_sent_at IS NULL
`

type MarkNotificationPushSentParams struct {
	SentAt         time.Time
	NotificationID uuid.UUID
}

func (q *Queries) MarkNotificationPushSent(ctx context.Context, arg MarkNotificationPushSentParams) (int64, error) {
	result, err := q.db.Exec(ctx, markNotificationPushSent, arg.SentAt, arg.NotificationID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const registerNotificationPushDevice = `-- name: RegisterNotificationPushDevice :one
WITH authorized_user AS (
    SELECT users.user_id
    FROM public.users
    WHERE users.user_id = CAST($3 AS uuid)
      AND users.is_active = TRUE
      AND users.is_system = FALSE
)
INSERT INTO public.notification_push_devices (
    user_id,
    expo_push_token,
    platform
)
SELECT
    authorized_user.user_id,
    CAST($1 AS text),
    CAST($2 AS text)
FROM authorized_user
ON CONFLICT (expo_push_token) DO UPDATE
SET
    user_id = EXCLUDED.user_id,
    platform = EXCLUDED.platform,
    updated_at = now(),
    disabled_at = NULL
RETURNING device_id, user_id, expo_push_token, platform, created_at, updated_at, disabled_at
`

type RegisterNotificationPushDeviceParams struct {
	ExpoPushToken string
	Platform      string
	UserID        uuid.UUID
}

func (q *Queries) RegisterNotificationPushDevice(ctx context.Context, arg RegisterNotificationPushDeviceParams) (NotificationPushDevice, error) {
	row := q.db.QueryRow(ctx, registerNotificationPushDevice, arg.ExpoPushToken, arg.Platform, arg.UserID)
	var i NotificationPushDevice
	err := row.Scan(
		&i.DeviceID,
		&i.UserID,
		&i.ExpoPushToken,
		&i.Platform,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DisabledAt,
	)
	return i, err
}

const unregisterNotificationPushDevice = `-- name: UnregisterNotificationPushDevice :execrows
DELETE FROM public.notification_push_devices
WHERE user_id = CAST($1 AS uuid)
  AND expo_push_token = CAST($2 AS text)
`

type UnregisterNotificationPushDeviceParams struct {
	UserID        uuid.UUID
	ExpoPushToken string
}

func (q *Queries) UnregisterNotificationPushDevice(ctx context.Context, arg UnregisterNotificationPushDeviceParams) (int64, error) {
	result, err := q.db.Exec(ctx, unregisterNotificationPushDevice, arg.UserID, arg.ExpoPushToken)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
