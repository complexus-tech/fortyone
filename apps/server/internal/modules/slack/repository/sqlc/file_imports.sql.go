// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: file_imports.sql

package slacksql

import (
	"context"

	"github.com/google/uuid"
)

const authorizeSlackFileImport = `-- name: AuthorizeSlackFileImport :one
SELECT EXISTS (
    SELECT 1
    FROM public.slack_file_imports AS file_import
    JOIN public.slack_workspaces AS installation
      ON installation.id = file_import.slack_workspace_id
     AND installation.workspace_id = file_import.workspace_id
     AND installation.installation_generation = file_import.installation_generation
     AND installation.slack_team_id = file_import.slack_team_id
     AND installation.is_active = TRUE
    JOIN public.workspaces AS workspace
      ON workspace.workspace_id = file_import.workspace_id AND workspace.deleted_at IS NULL
    JOIN public.slack_user_links AS link
      ON link.workspace_id = file_import.workspace_id
     AND link.slack_workspace_id = installation.id
     AND link.slack_team_id = file_import.slack_team_id
     AND link.slack_user_id = file_import.slack_user_id
     AND link.user_id = file_import.actor_id
    JOIN public.workspace_members AS membership
      ON membership.workspace_id = file_import.workspace_id
     AND membership.user_id = file_import.actor_id
     AND membership.role IN ('admin', 'member')
    JOIN public.users AS actor
      ON actor.user_id = file_import.actor_id AND actor.is_active = TRUE
    JOIN public.stories AS story
      ON story.id = file_import.story_id
     AND story.workspace_id = file_import.workspace_id
     AND story.deleted_at IS NULL
    JOIN public.team_members AS team_member
      ON team_member.team_id = story.team_id AND team_member.user_id = file_import.actor_id
    WHERE file_import.id = CAST($1 AS uuid)
      AND file_import.status = 'processing'
      AND POSITION(',files:read,' IN ',' || REPLACE(COALESCE(installation.scope, ''), ' ', '') || ',') > 0
)
`

type AuthorizeSlackFileImportParams struct {
	ImportID uuid.UUID
}

func (q *Queries) AuthorizeSlackFileImport(ctx context.Context, arg AuthorizeSlackFileImportParams) (bool, error) {
	row := q.db.QueryRow(ctx, authorizeSlackFileImport, arg.ImportID)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const cancelSlackFileImport = `-- name: CancelSlackFileImport :execrows
UPDATE public.slack_file_imports
SET status = 'cancelled', lease_until = NULL,
    last_error = 'Slack access or story permission is no longer available', updated_at = now()
WHERE id = CAST($1 AS uuid)
  AND status = 'processing'
  AND attempt_count = CAST($2 AS integer)
`

type CancelSlackFileImportParams struct {
	ImportID     uuid.UUID
	AttemptCount int32
}

func (q *Queries) CancelSlackFileImport(ctx context.Context, arg CancelSlackFileImportParams) (int64, error) {
	result, err := q.db.Exec(ctx, cancelSlackFileImport, arg.ImportID, arg.AttemptCount)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const claimSlackFileImport = `-- name: ClaimSlackFileImport :one
UPDATE public.slack_file_imports
SET status = 'processing',
    attempt_count = attempt_count + 1,
    lease_until = now() + INTERVAL '90 minutes',
    last_error = NULL,
    updated_at = now()
WHERE id = CAST($1 AS uuid)
  AND attempt_count < 8
  AND (
    status IN ('pending', 'failed')
    OR (status = 'processing' AND lease_until < now())
  )
RETURNING
    id, workspace_id, slack_workspace_id, installation_generation, slack_team_id,
    slack_user_id, slack_channel_id, slack_thread_ts, slack_message_ts,
    slack_file_id, story_id, actor_id, attempt_count
`

type ClaimSlackFileImportParams struct {
	ImportID uuid.UUID
}

type ClaimSlackFileImportRow struct {
	ID                     uuid.UUID
	WorkspaceID            uuid.UUID
	SlackWorkspaceID       uuid.UUID
	InstallationGeneration uuid.UUID
	SlackTeamID            string
	SlackUserID            string
	SlackChannelID         string
	SlackThreadTs          string
	SlackMessageTs         string
	SlackFileID            string
	StoryID                uuid.UUID
	ActorID                uuid.UUID
	AttemptCount           int32
}

func (q *Queries) ClaimSlackFileImport(ctx context.Context, arg ClaimSlackFileImportParams) (ClaimSlackFileImportRow, error) {
	row := q.db.QueryRow(ctx, claimSlackFileImport, arg.ImportID)
	var i ClaimSlackFileImportRow
	err := row.Scan(
		&i.ID,
		&i.WorkspaceID,
		&i.SlackWorkspaceID,
		&i.InstallationGeneration,
		&i.SlackTeamID,
		&i.SlackUserID,
		&i.SlackChannelID,
		&i.SlackThreadTs,
		&i.SlackMessageTs,
		&i.SlackFileID,
		&i.StoryID,
		&i.ActorID,
		&i.AttemptCount,
	)
	return i, err
}

const completeSlackFileImport = `-- name: CompleteSlackFileImport :execrows
UPDATE public.slack_file_imports
SET status = 'complete', attachment_id = CAST($1 AS uuid),
    lease_until = NULL, last_error = NULL, updated_at = now()
WHERE id = CAST($2 AS uuid)
  AND status = 'processing'
  AND attempt_count = CAST($3 AS integer)
`

type CompleteSlackFileImportParams struct {
	AttachmentID uuid.UUID
	ImportID     uuid.UUID
	AttemptCount int32
}

func (q *Queries) CompleteSlackFileImport(ctx context.Context, arg CompleteSlackFileImportParams) (int64, error) {
	result, err := q.db.Exec(ctx, completeSlackFileImport, arg.AttachmentID, arg.ImportID, arg.AttemptCount)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const failSlackFileImport = `-- name: FailSlackFileImport :execrows
UPDATE public.slack_file_imports
SET status = 'failed', lease_until = NULL,
    last_error = CASE
        WHEN attempt_count >= 8 THEN 'Slack file import failed after retries'
        ELSE 'Slack file import failed; retry scheduled'
    END,
    updated_at = now()
WHERE id = CAST($1 AS uuid)
  AND status = 'processing'
  AND attempt_count = CAST($2 AS integer)
`

type FailSlackFileImportParams struct {
	ImportID     uuid.UUID
	AttemptCount int32
}

func (q *Queries) FailSlackFileImport(ctx context.Context, arg FailSlackFileImportParams) (int64, error) {
	result, err := q.db.Exec(ctx, failSlackFileImport, arg.ImportID, arg.AttemptCount)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const listRecoverableSlackFileImports = `-- name: ListRecoverableSlackFileImports :many
SELECT id
FROM public.slack_file_imports
WHERE attempt_count < 8
  AND (
    status IN ('pending', 'failed')
    OR (status = 'processing' AND lease_until < now())
  )
ORDER BY updated_at, id
LIMIT CAST($1 AS integer)
`

type ListRecoverableSlackFileImportsParams struct {
	ResultLimit int32
}

func (q *Queries) ListRecoverableSlackFileImports(ctx context.Context, arg ListRecoverableSlackFileImportsParams) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, listRecoverableSlackFileImports, arg.ResultLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []uuid.UUID{}
	for rows.Next() {
		var id uuid.UUID
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const registerSlackFileImport = `-- name: RegisterSlackFileImport :one
INSERT INTO public.slack_file_imports (
    workspace_id, slack_workspace_id, installation_generation, slack_team_id,
    slack_user_id, slack_channel_id, slack_thread_ts, slack_message_ts,
    slack_file_id, idempotency_key, story_id, actor_id
)
SELECT
    installation.workspace_id, installation.id, installation.installation_generation,
    installation.slack_team_id, CAST($1 AS text),
    CAST($2 AS text), CAST($3 AS text),
    CAST($4 AS text), CAST($5 AS text),
    CAST($6 AS text), story.id, actor.user_id
FROM public.slack_workspaces AS installation
JOIN public.workspaces AS workspace
  ON workspace.workspace_id = installation.workspace_id AND workspace.deleted_at IS NULL
JOIN public.slack_user_links AS link
  ON link.workspace_id = installation.workspace_id
 AND link.slack_workspace_id = installation.id
 AND link.slack_team_id = installation.slack_team_id
 AND link.slack_user_id = CAST($1 AS text)
 AND link.user_id = CAST($7 AS uuid)
JOIN public.workspace_members AS membership
  ON membership.workspace_id = installation.workspace_id
 AND membership.user_id = link.user_id
 AND membership.role IN ('admin', 'member')
JOIN public.users AS actor
  ON actor.user_id = membership.user_id AND actor.is_active = TRUE
JOIN public.stories AS story
  ON story.id = CAST($8 AS uuid)
 AND story.workspace_id = installation.workspace_id
 AND story.deleted_at IS NULL
JOIN public.team_members AS team_member
  ON team_member.team_id = story.team_id AND team_member.user_id = actor.user_id
WHERE installation.id = CAST($9 AS uuid)
  AND installation.workspace_id = CAST($10 AS uuid)
  AND installation.installation_generation = CAST($11 AS uuid)
  AND installation.slack_team_id = CAST($12 AS text)
  AND installation.is_active = TRUE
  AND POSITION(',files:read,' IN ',' || REPLACE(COALESCE(installation.scope, ''), ' ', '') || ',') > 0
ON CONFLICT (workspace_id, story_id, slack_file_id)
DO UPDATE SET
    slack_workspace_id = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.slack_workspace_id
        ELSE EXCLUDED.slack_workspace_id
    END,
    installation_generation = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.installation_generation
        ELSE EXCLUDED.installation_generation
    END,
    slack_team_id = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.slack_team_id
        ELSE EXCLUDED.slack_team_id
    END,
    slack_user_id = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.slack_user_id
        ELSE EXCLUDED.slack_user_id
    END,
    slack_channel_id = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.slack_channel_id
        ELSE EXCLUDED.slack_channel_id
    END,
    slack_thread_ts = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.slack_thread_ts
        ELSE EXCLUDED.slack_thread_ts
    END,
    slack_message_ts = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.slack_message_ts
        ELSE EXCLUDED.slack_message_ts
    END,
    actor_id = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.actor_id
        ELSE EXCLUDED.actor_id
    END,
    status = CASE
        WHEN public.slack_file_imports.status = 'complete' THEN 'complete'
        WHEN public.slack_file_imports.status = 'processing'
         AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation THEN 'processing'
        ELSE 'pending'
    END,
    attempt_count = CASE
        WHEN public.slack_file_imports.status = 'complete'
          OR (public.slack_file_imports.status = 'processing'
              AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation)
          THEN public.slack_file_imports.attempt_count
        ELSE 0
    END,
    lease_until = CASE
        WHEN public.slack_file_imports.status = 'processing'
         AND public.slack_file_imports.installation_generation = EXCLUDED.installation_generation
          THEN public.slack_file_imports.lease_until
        ELSE NULL
    END,
    last_error = CASE
        WHEN public.slack_file_imports.status = 'complete' THEN public.slack_file_imports.last_error
        ELSE NULL
    END,
    updated_at = now()
RETURNING id
`

type RegisterSlackFileImportParams struct {
	SlackUserID            string
	SlackChannelID         string
	SlackThreadTs          string
	SlackMessageTs         string
	SlackFileID            string
	IdempotencyKey         string
	ActorID                uuid.UUID
	StoryID                uuid.UUID
	SlackWorkspaceID       uuid.UUID
	WorkspaceID            uuid.UUID
	InstallationGeneration uuid.UUID
	SlackTeamID            string
}

func (q *Queries) RegisterSlackFileImport(ctx context.Context, arg RegisterSlackFileImportParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, registerSlackFileImport,
		arg.SlackUserID,
		arg.SlackChannelID,
		arg.SlackThreadTs,
		arg.SlackMessageTs,
		arg.SlackFileID,
		arg.IdempotencyKey,
		arg.ActorID,
		arg.StoryID,
		arg.SlackWorkspaceID,
		arg.WorkspaceID,
		arg.InstallationGeneration,
		arg.SlackTeamID,
	)
	var id uuid.UUID
	err := row.Scan(&id)
	return id, err
}
