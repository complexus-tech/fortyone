// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: strategy_communications.sql

package objectivessql

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const getStrategyCommunicationFoundation = `-- name: GetStrategyCommunicationFoundation :one
SELECT
    EXISTS (
        SELECT 1
        FROM public.workspace_strategies AS strategy
        WHERE strategy.workspace_id = $1
          AND NULLIF(TRIM(strategy.ultimate_goal), '') IS NOT NULL
    ) AS has_ultimate_goal,
    (
        SELECT COUNT(*)
        FROM public.strategic_pillars AS pillar
        WHERE pillar.workspace_id = $1
    ) AS pillar_count,
    (
        SELECT COUNT(*)
        FROM public.objectives AS objective
        LEFT JOIN public.objective_statuses AS status
            ON status.status_id = objective.status_id
        WHERE objective.workspace_id = $1
          AND (
              objective.start_date IS NULL
              OR objective.start_date < CAST($2 AS date)
          )
          AND (
              objective.end_date IS NULL
              OR objective.end_date >= CAST($3 AS date)
          )
          AND COALESCE(status.category, '') NOT IN ('completed', 'cancelled', 'paused')
    ) AS objective_count
`

type GetStrategyCommunicationFoundationParams struct {
	WorkspaceID uuid.UUID
	PeriodEnd   time.Time
	PeriodStart time.Time
}

type GetStrategyCommunicationFoundationRow struct {
	HasUltimateGoal bool
	PillarCount     int64
	ObjectiveCount  int64
}

func (q *Queries) GetStrategyCommunicationFoundation(ctx context.Context, arg GetStrategyCommunicationFoundationParams) (GetStrategyCommunicationFoundationRow, error) {
	row := q.db.QueryRow(ctx, getStrategyCommunicationFoundation, arg.WorkspaceID, arg.PeriodEnd, arg.PeriodStart)
	var i GetStrategyCommunicationFoundationRow
	err := row.Scan(&i.HasUltimateGoal, &i.PillarCount, &i.ObjectiveCount)
	return i, err
}

const getStrategyCommunicationMonthlySummary = `-- name: GetStrategyCommunicationMonthlySummary :one
WITH objective_data AS (
    SELECT
        objective.objective_id,
        CAST(objective.health AS text) AS health,
        alignment.pillar_id
    FROM public.objectives AS objective
    LEFT JOIN public.objective_statuses AS status
        ON status.status_id = objective.status_id
    LEFT JOIN public.strategy_objective_alignments AS alignment
        ON alignment.objective_id = objective.objective_id
    WHERE objective.workspace_id = $1
      AND COALESCE(status.category, '') NOT IN ('completed', 'cancelled', 'paused')
),
key_result_data AS (
    SELECT
        CASE
            WHEN CAST(key_result.measurement_type AS text) = 'percentage' THEN
                GREATEST(0.0, LEAST(100.0, key_result.current_value))
            WHEN CAST(key_result.measurement_type AS text) = 'number'
                 AND key_result.target_value = key_result.start_value THEN
                CASE
                    WHEN key_result.current_value = key_result.target_value THEN 100.0
                    ELSE 0.0
                END
            WHEN CAST(key_result.measurement_type AS text) = 'number' THEN
                GREATEST(
                    0.0,
                    LEAST(
                        100.0,
                        (
                            (key_result.current_value - key_result.start_value)
                            / NULLIF(key_result.target_value - key_result.start_value, 0)
                        ) * 100.0
                    )
                )
            WHEN CAST(key_result.measurement_type AS text) = 'boolean' THEN
                CASE
                    WHEN key_result.current_value = key_result.target_value THEN 100.0
                    ELSE 0.0
                END
            ELSE NULL
        END AS progress
    FROM public.key_results AS key_result
    INNER JOIN objective_data AS objective
        ON objective.objective_id = key_result.objective_id
)
SELECT
    (
        SELECT COUNT(*)
        FROM public.strategic_pillars AS pillar
        WHERE pillar.workspace_id = $1
    ) AS pillar_count,
    (
        SELECT COUNT(DISTINCT pillar_id)
        FROM objective_data
        WHERE pillar_id IS NOT NULL
          AND health IN ('At Risk', 'Off Track')
    ) AS pillars_needing_review,
    (SELECT COUNT(*) FROM objective_data) AS objective_count,
    (
        SELECT COUNT(*)
        FROM objective_data
        WHERE health IN ('At Risk', 'Off Track')
    ) AS at_risk_objectives,
    (
        SELECT COUNT(*)
        FROM objective_data
        WHERE pillar_id IS NULL
    ) AS unaligned_objectives,
    (SELECT COUNT(*) FROM key_result_data) AS key_result_count,
    (SELECT COUNT(progress) FROM key_result_data) AS key_result_progress_count,
    CAST(
        COALESCE(
            (SELECT AVG(progress) FROM key_result_data),
            CAST(0 AS numeric)
        )
        AS double precision
    ) AS key_result_progress,
    (
        SELECT COUNT(*)
        FROM public.stories AS story
        WHERE story.workspace_id = $1
          AND (story.objective_id IS NOT NULL OR story.key_result_id IS NOT NULL)
          AND story.completed_at >= CAST($2 AS timestamptz)
          AND story.completed_at < CAST($3 AS timestamptz)
          AND story.deleted_at IS NULL
    ) AS completed_stories
`

type GetStrategyCommunicationMonthlySummaryParams struct {
	WorkspaceID uuid.UUID
	PeriodStart time.Time
	PeriodEnd   time.Time
}

type GetStrategyCommunicationMonthlySummaryRow struct {
	PillarCount            int64
	PillarsNeedingReview   int64
	ObjectiveCount         int64
	AtRiskObjectives       int64
	UnalignedObjectives    int64
	KeyResultCount         int64
	KeyResultProgressCount int64
	KeyResultProgress      float64
	CompletedStories       int64
}

func (q *Queries) GetStrategyCommunicationMonthlySummary(ctx context.Context, arg GetStrategyCommunicationMonthlySummaryParams) (GetStrategyCommunicationMonthlySummaryRow, error) {
	row := q.db.QueryRow(ctx, getStrategyCommunicationMonthlySummary, arg.WorkspaceID, arg.PeriodStart, arg.PeriodEnd)
	var i GetStrategyCommunicationMonthlySummaryRow
	err := row.Scan(
		&i.PillarCount,
		&i.PillarsNeedingReview,
		&i.ObjectiveCount,
		&i.AtRiskObjectives,
		&i.UnalignedObjectives,
		&i.KeyResultCount,
		&i.KeyResultProgressCount,
		&i.KeyResultProgress,
		&i.CompletedStories,
	)
	return i, err
}

const listStrategyCommunicationAdministrators = `-- name: ListStrategyCommunicationAdministrators :many
SELECT
    user_account.user_id,
    membership.workspace_id,
    CAST(COALESCE(NULLIF(TRIM(user_account.timezone), ''), 'UTC') AS text) AS timezone
FROM public.workspace_members AS membership
INNER JOIN public.users AS user_account
    ON user_account.user_id = membership.user_id
   AND user_account.is_active = true
   AND user_account.is_system = false
INNER JOIN public.workspaces AS workspace
    ON workspace.workspace_id = membership.workspace_id
   AND workspace.deleted_at IS NULL
WHERE membership.role = 'admin'
  AND (
      NOT CAST($1 AS boolean)
      OR membership.workspace_id > $2
      OR (
          membership.workspace_id = $2
          AND user_account.user_id > $3
      )
  )
ORDER BY membership.workspace_id, user_account.user_id
LIMIT CAST($4 AS integer)
`

type ListStrategyCommunicationAdministratorsParams struct {
	HasCursor        bool
	AfterWorkspaceID uuid.UUID
	AfterUserID      uuid.UUID
	ResultLimit      int32
}

type ListStrategyCommunicationAdministratorsRow struct {
	UserID      uuid.UUID
	WorkspaceID uuid.UUID
	Timezone    string
}

func (q *Queries) ListStrategyCommunicationAdministrators(ctx context.Context, arg ListStrategyCommunicationAdministratorsParams) ([]ListStrategyCommunicationAdministratorsRow, error) {
	rows, err := q.db.Query(ctx, listStrategyCommunicationAdministrators,
		arg.HasCursor,
		arg.AfterWorkspaceID,
		arg.AfterUserID,
		arg.ResultLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStrategyCommunicationAdministratorsRow{}
	for rows.Next() {
		var i ListStrategyCommunicationAdministratorsRow
		if err := rows.Scan(&i.UserID, &i.WorkspaceID, &i.Timezone); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStrategyWeeklyCommunicationRecipients = `-- name: ListStrategyWeeklyCommunicationRecipients :many
SELECT
    user_account.user_id,
    membership.workspace_id,
    CAST(COALESCE(NULLIF(TRIM(user_account.timezone), ''), 'UTC') AS text) AS timezone
FROM public.workspace_members AS membership
INNER JOIN public.users AS user_account
    ON user_account.user_id = membership.user_id
   AND user_account.is_active = true
   AND user_account.is_system = false
INNER JOIN public.workspaces AS workspace
    ON workspace.workspace_id = membership.workspace_id
   AND workspace.deleted_at IS NULL
WHERE membership.role IN ('admin', 'member', 'guest')
  AND (
      NOT CAST($1 AS boolean)
      OR membership.workspace_id > $2
      OR (
          membership.workspace_id = $2
          AND user_account.user_id > $3
      )
  )
ORDER BY membership.workspace_id, user_account.user_id
LIMIT CAST($4 AS integer)
`

type ListStrategyWeeklyCommunicationRecipientsParams struct {
	HasCursor        bool
	AfterWorkspaceID uuid.UUID
	AfterUserID      uuid.UUID
	ResultLimit      int32
}

type ListStrategyWeeklyCommunicationRecipientsRow struct {
	UserID      uuid.UUID
	WorkspaceID uuid.UUID
	Timezone    string
}

func (q *Queries) ListStrategyWeeklyCommunicationRecipients(ctx context.Context, arg ListStrategyWeeklyCommunicationRecipientsParams) ([]ListStrategyWeeklyCommunicationRecipientsRow, error) {
	rows, err := q.db.Query(ctx, listStrategyWeeklyCommunicationRecipients,
		arg.HasCursor,
		arg.AfterWorkspaceID,
		arg.AfterUserID,
		arg.ResultLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStrategyWeeklyCommunicationRecipientsRow{}
	for rows.Next() {
		var i ListStrategyWeeklyCommunicationRecipientsRow
		if err := rows.Scan(&i.UserID, &i.WorkspaceID, &i.Timezone); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStrategyWeeklyCommunicationRecords = `-- name: ListStrategyWeeklyCommunicationRecords :many
WITH recipient AS MATERIALIZED (
    SELECT
        user_account.user_id,
        membership.workspace_id,
        membership.role,
        CAST(COALESCE(NULLIF(TRIM(user_account.timezone), ''), 'UTC') AS text) AS timezone
    FROM public.workspace_members AS membership
    INNER JOIN public.users AS user_account
        ON user_account.user_id = membership.user_id
       AND user_account.is_active = true
       AND user_account.is_system = false
    INNER JOIN public.workspaces AS workspace
        ON workspace.workspace_id = membership.workspace_id
       AND workspace.deleted_at IS NULL
    WHERE membership.user_id = $2
      AND membership.workspace_id = $3
),
eligible_objectives AS (
    SELECT
        recipient.user_id,
        recipient.workspace_id,
        recipient.timezone,
        objective.objective_id,
        objective.team_id,
        objective.name AS objective_name,
        objective.health IS NOT NULL AS objective_health_is_set,
        COALESCE(CAST(objective.health AS text), '') AS objective_health,
        status.status_id AS objective_status_id,
        status.name AS objective_status_name,
        status.category AS objective_status_category,
        objective.start_date AS objective_start_date,
        objective.end_date AS objective_end_date,
        objective.updated_at AS objective_updated_at,
        objective.updated_at < CAST($4 AS timestamptz) AS is_stale_objective,
        COALESCE(CAST(objective.health AS text) IN ('At Risk', 'Off Track'), false) AS is_at_risk_objective
    FROM recipient
    INNER JOIN public.objectives AS objective
        ON objective.workspace_id = recipient.workspace_id
       AND objective.lead_user_id = recipient.user_id
    LEFT JOIN public.objective_statuses AS status
        ON status.status_id = objective.status_id
    WHERE objective.team_id IS NOT NULL
      AND COALESCE(status.category, '') NOT IN ('completed', 'cancelled', 'paused')
      AND (
          recipient.role = 'admin'
          OR EXISTS (
              SELECT 1
              FROM public.team_members AS team_membership
              WHERE team_membership.team_id = objective.team_id
                AND team_membership.user_id = recipient.user_id
          )
      )
),
stale_key_results AS (
    SELECT
        key_result.id,
        key_result.objective_id,
        key_result.name,
        CAST(key_result.measurement_type AS text) AS measurement_type,
        CAST(key_result.start_value AS double precision) AS start_value,
        CAST(key_result.current_value AS double precision) AS current_value,
        CAST(key_result.target_value AS double precision) AS target_value,
        key_result.start_date,
        key_result.end_date,
        key_result.updated_at
    FROM public.key_results AS key_result
    INNER JOIN eligible_objectives AS objective
        ON objective.objective_id = key_result.objective_id
    WHERE key_result.updated_at < CAST($4 AS timestamptz)
      AND NOT COALESCE(
          CASE
              WHEN CAST(key_result.measurement_type AS text) IN ('percentage', 'number')
                   AND key_result.target_value >= key_result.start_value THEN
                  key_result.current_value >= key_result.target_value
              WHEN CAST(key_result.measurement_type AS text) IN ('percentage', 'number')
                   AND key_result.target_value < key_result.start_value THEN
                  key_result.current_value <= key_result.target_value
              WHEN CAST(key_result.measurement_type AS text) = 'boolean' THEN
                  key_result.current_value = key_result.target_value
              ELSE false
          END,
          false
      )
),
signal_rows AS (
    SELECT
        objective.user_id,
        objective.workspace_id,
        objective.timezone,
        objective.objective_id,
        objective.team_id,
        objective.objective_name,
        objective.objective_health_is_set,
        objective.objective_health,
        objective.objective_status_id,
        objective.objective_status_name,
        objective.objective_status_category,
        objective.objective_start_date,
        objective.objective_end_date,
        objective.objective_updated_at,
        objective.is_stale_objective,
        objective.is_at_risk_objective,
        key_result.id AS key_result_id,
        key_result.name AS key_result_name,
        key_result.measurement_type AS key_result_measurement_type,
        key_result.start_value AS key_result_start_value,
        key_result.current_value AS key_result_current_value,
        key_result.target_value AS key_result_target_value,
        key_result.start_date AS key_result_start_date,
        key_result.end_date AS key_result_end_date,
        key_result.updated_at AS key_result_updated_at,
        CASE WHEN key_result.id IS NULL THEN 1 ELSE 0 END AS key_result_null_rank,
        COALESCE(
            key_result.id,
            CAST('00000000-0000-0000-0000-000000000000' AS uuid)
        ) AS key_result_sort_id
    FROM eligible_objectives AS objective
    LEFT JOIN stale_key_results AS key_result
        ON key_result.objective_id = objective.objective_id
    WHERE (
        objective.is_stale_objective
        OR objective.is_at_risk_objective
        OR key_result.id IS NOT NULL
    )
      AND (
          NOT CAST($5 AS boolean)
          OR objective.objective_id > $6
          OR (
              objective.objective_id = $6
              AND CASE WHEN key_result.id IS NULL THEN 1 ELSE 0 END
                  > CAST($7 AS integer)
          )
          OR (
              objective.objective_id = $6
              AND CASE WHEN key_result.id IS NULL THEN 1 ELSE 0 END
                  = CAST($7 AS integer)
              AND COALESCE(
                  key_result.id,
                  CAST('00000000-0000-0000-0000-000000000000' AS uuid)
              ) > $8
          )
      )
)
SELECT
    communication_signal.user_id,
    communication_signal.workspace_id,
    communication_signal.timezone,
    communication_signal.objective_id,
    communication_signal.team_id,
    communication_signal.objective_name,
    CAST(communication_signal.objective_health_is_set AS boolean) AS objective_health_is_set,
    CAST(communication_signal.objective_health AS text) AS objective_health,
    communication_signal.objective_status_id,
    communication_signal.objective_status_name,
    communication_signal.objective_status_category,
    communication_signal.objective_start_date,
    communication_signal.objective_end_date,
    communication_signal.objective_updated_at,
    communication_signal.is_stale_objective,
    CAST(communication_signal.is_at_risk_objective AS boolean) AS is_at_risk_objective,
    communication_signal.key_result_id,
    communication_signal.key_result_name,
    communication_signal.key_result_measurement_type,
    communication_signal.key_result_start_value,
    communication_signal.key_result_current_value,
    communication_signal.key_result_target_value,
    communication_signal.key_result_start_date,
    communication_signal.key_result_end_date,
    communication_signal.key_result_updated_at,
    communication_signal.key_result_null_rank,
    communication_signal.key_result_sort_id
FROM signal_rows AS communication_signal
ORDER BY
    communication_signal.objective_id,
    communication_signal.key_result_null_rank,
    communication_signal.key_result_sort_id
LIMIT CAST($1 AS integer)
`

type ListStrategyWeeklyCommunicationRecordsParams struct {
	ResultLimit            int32
	UserID                 uuid.UUID
	WorkspaceID            uuid.UUID
	StaleBefore            time.Time
	HasCursor              bool
	AfterObjectiveID       uuid.UUID
	AfterKeyResultNullRank int32
	AfterKeyResultID       uuid.UUID
}

type ListStrategyWeeklyCommunicationRecordsRow struct {
	UserID                   uuid.UUID
	WorkspaceID              uuid.UUID
	Timezone                 string
	ObjectiveID              uuid.UUID
	TeamID                   *uuid.UUID
	ObjectiveName            string
	ObjectiveHealthIsSet     bool
	ObjectiveHealth          string
	ObjectiveStatusID        *uuid.UUID
	ObjectiveStatusName      *string
	ObjectiveStatusCategory  *string
	ObjectiveStartDate       *time.Time
	ObjectiveEndDate         *time.Time
	ObjectiveUpdatedAt       time.Time
	IsStaleObjective         bool
	IsAtRiskObjective        bool
	KeyResultID              *uuid.UUID
	KeyResultName            *string
	KeyResultMeasurementType *string
	KeyResultStartValue      *float64
	KeyResultCurrentValue    *float64
	KeyResultTargetValue     *float64
	KeyResultStartDate       *time.Time
	KeyResultEndDate         *time.Time
	KeyResultUpdatedAt       *time.Time
	KeyResultNullRank        int32
	KeyResultSortID          uuid.UUID
}

func (q *Queries) ListStrategyWeeklyCommunicationRecords(ctx context.Context, arg ListStrategyWeeklyCommunicationRecordsParams) ([]ListStrategyWeeklyCommunicationRecordsRow, error) {
	rows, err := q.db.Query(ctx, listStrategyWeeklyCommunicationRecords,
		arg.ResultLimit,
		arg.UserID,
		arg.WorkspaceID,
		arg.StaleBefore,
		arg.HasCursor,
		arg.AfterObjectiveID,
		arg.AfterKeyResultNullRank,
		arg.AfterKeyResultID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStrategyWeeklyCommunicationRecordsRow{}
	for rows.Next() {
		var i ListStrategyWeeklyCommunicationRecordsRow
		if err := rows.Scan(
			&i.UserID,
			&i.WorkspaceID,
			&i.Timezone,
			&i.ObjectiveID,
			&i.TeamID,
			&i.ObjectiveName,
			&i.ObjectiveHealthIsSet,
			&i.ObjectiveHealth,
			&i.ObjectiveStatusID,
			&i.ObjectiveStatusName,
			&i.ObjectiveStatusCategory,
			&i.ObjectiveStartDate,
			&i.ObjectiveEndDate,
			&i.ObjectiveUpdatedAt,
			&i.IsStaleObjective,
			&i.IsAtRiskObjective,
			&i.KeyResultID,
			&i.KeyResultName,
			&i.KeyResultMeasurementType,
			&i.KeyResultStartValue,
			&i.KeyResultCurrentValue,
			&i.KeyResultTargetValue,
			&i.KeyResultStartDate,
			&i.KeyResultEndDate,
			&i.KeyResultUpdatedAt,
			&i.KeyResultNullRank,
			&i.KeyResultSortID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
