// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: objectives.sql

package objectivessql

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const canReadObjective = `-- name: CanReadObjective :one
SELECT EXISTS (
    SELECT 1
    FROM public.objectives AS objective
    INNER JOIN public.workspace_members AS membership
        ON membership.workspace_id = objective.workspace_id
       AND membership.user_id = $1
    INNER JOIN public.team_members AS team_membership
        ON team_membership.team_id = objective.team_id
       AND team_membership.user_id = $1
    INNER JOIN public.users AS actor
        ON actor.user_id = membership.user_id
       AND actor.is_active = TRUE
    WHERE objective.objective_id = $2
      AND objective.workspace_id = $3
	  AND membership.role IN ('member', 'admin')
) AS can_read
`

type CanReadObjectiveParams struct {
	ActorID     uuid.UUID
	ObjectiveID uuid.UUID
	WorkspaceID *uuid.UUID
}

func (q *Queries) CanReadObjective(ctx context.Context, arg CanReadObjectiveParams) (bool, error) {
	row := q.db.QueryRow(ctx, canReadObjective, arg.ActorID, arg.ObjectiveID, arg.WorkspaceID)
	var can_read bool
	err := row.Scan(&can_read)
	return can_read, err
}

const getObjective = `-- name: GetObjective :one
WITH visible_objective AS (
    SELECT objective.objective_id, objective.name, objective.description, objective.lead_user_id, objective.team_id, objective.workspace_id, objective.start_date, objective.end_date, objective.is_private, objective.created_at, objective.updated_at, objective.status_id, objective.priority, objective.health, objective.created_by, objective.search_vector, objective.short_summary, objective.sequence_id, objective.color
    FROM public.objectives AS objective
    WHERE objective.objective_id = $1
      AND objective.workspace_id = $2
      AND (
          CAST($3 AS boolean)
          OR EXISTS (
              SELECT 1
              FROM public.workspace_members AS membership
              INNER JOIN public.team_members AS team_membership
                  ON team_membership.team_id = objective.team_id
                 AND team_membership.user_id = membership.user_id
              INNER JOIN public.users AS actor
                  ON actor.user_id = membership.user_id
                 AND actor.is_active = TRUE
              WHERE membership.workspace_id = objective.workspace_id
                AND membership.user_id = $4
				AND membership.role IN ('member', 'admin')
          )
      )
), story_schedule AS (
    SELECT
        story.id,
        story.sequence_id,
        story.title,
        story.objective_id,
        CASE
            WHEN MIN(block.start_at) IS NULL THEN story.start_date
            WHEN story.start_date IS NULL THEN CAST(MIN(block.start_at) AS date)
            ELSE LEAST(story.start_date, CAST(MIN(block.start_at) AS date))
        END AS forecast_start_date,
        CASE
            WHEN MAX(block.end_at) IS NULL THEN story.end_date
            WHEN story.end_date IS NULL THEN CAST(MAX(block.end_at) AS date)
            ELSE GREATEST(story.end_date, CAST(MAX(block.end_at) AS date))
        END AS forecast_end_date,
        CASE
            WHEN MAX(block.end_at) IS NOT NULL
                AND (story.end_date IS NULL OR CAST(MAX(block.end_at) AS date) >= story.end_date)
            THEN 'calendar'
            ELSE 'planning'
        END AS forecast_source
    FROM public.stories AS story
    INNER JOIN public.statuses AS status
        ON status.status_id = story.status_id
       AND status.workspace_id = story.workspace_id
    LEFT JOIN public.calendar_schedule_blocks AS block
        ON block.story_id = story.id
       AND block.workspace_id = story.workspace_id
       AND block.block_type = 'work'
    INNER JOIN visible_objective AS objective
        ON objective.objective_id = story.objective_id
       AND objective.workspace_id = story.workspace_id
    WHERE story.deleted_at IS NULL
      AND story.archived_at IS NULL
      AND status.category NOT IN ('completed', 'cancelled')
    GROUP BY story.id, story.sequence_id, story.title, story.objective_id, story.start_date, story.end_date
), ranked_story_schedule AS (
    SELECT
        story_schedule.id, story_schedule.sequence_id, story_schedule.title, story_schedule.objective_id, story_schedule.forecast_start_date, story_schedule.forecast_end_date, story_schedule.forecast_source,
        ROW_NUMBER() OVER (
            PARTITION BY objective_id
            ORDER BY forecast_end_date DESC, sequence_id DESC, id
        ) AS forecast_rank
    FROM story_schedule
    WHERE forecast_end_date IS NOT NULL
), objective_schedule AS (
    SELECT objective_id, MIN(forecast_start_date) AS forecast_start_date, MAX(forecast_end_date) AS forecast_end_date
    FROM story_schedule
    GROUP BY objective_id
), story_stats AS (
    SELECT
        objective.objective_id,
        COUNT(story.id) AS total,
        COUNT(story.id) FILTER (WHERE status.category = 'cancelled') AS cancelled,
        COUNT(story.id) FILTER (WHERE status.category = 'completed') AS completed,
        COUNT(story.id) FILTER (WHERE status.category = 'started') AS started,
        COUNT(story.id) FILTER (WHERE status.category = 'unstarted') AS unstarted,
        COUNT(story.id) FILTER (WHERE status.category = 'backlog') AS backlog
    FROM visible_objective AS objective
    LEFT JOIN public.stories AS story
        ON story.objective_id = objective.objective_id
       AND story.workspace_id = objective.workspace_id
       AND story.deleted_at IS NULL
       AND story.archived_at IS NULL
    LEFT JOIN public.statuses AS status ON status.status_id = story.status_id
       AND status.workspace_id = story.workspace_id
    GROUP BY objective.objective_id
), key_result_stats AS (
    SELECT key_result.objective_id, COUNT(*) AS total
    FROM public.key_results AS key_result
    INNER JOIN visible_objective AS objective ON objective.objective_id = key_result.objective_id
    GROUP BY key_result.objective_id
)
SELECT
    objective.objective_id,
    objective.sequence_id,
    objective.name,
    objective.description,
    objective.short_summary,
    objective.lead_user_id,
    objective.team_id,
    objective.workspace_id,
    objective.start_date,
    objective.end_date,
    objective.is_private,
    objective.created_at,
    objective.updated_at,
    objective.status_id,
    objective.priority,
    CAST(COALESCE(CAST(objective.health AS text), '') AS text) AS health,
    objective.color,
    objective.created_by,
    CAST(COALESCE(key_result_stats.total, 0) AS integer) AS key_result_count,
    CAST(COALESCE(story_stats.total, 0) AS integer) AS total_stories,
    CAST(COALESCE(story_stats.cancelled, 0) AS integer) AS cancelled_stories,
    CAST(COALESCE(story_stats.completed, 0) AS integer) AS completed_stories,
    CAST(COALESCE(story_stats.started, 0) AS integer) AS started_stories,
    CAST(COALESCE(story_stats.unstarted, 0) AS integer) AS unstarted_stories,
    CAST(COALESCE(story_stats.backlog, 0) AS integer) AS backlog_stories,
    CAST(COALESCE(CAST(objective_schedule.forecast_start_date AS date), CAST('1970-01-01' AS date)) AS date) AS forecast_start_date,
    CAST(objective_schedule.forecast_start_date IS NOT NULL AS boolean) AS has_forecast_start_date,
    CAST(COALESCE(CAST(objective_schedule.forecast_end_date AS date), CAST('1970-01-01' AS date)) AS date) AS forecast_end_date,
    CAST(objective_schedule.forecast_end_date IS NOT NULL AS boolean) AS has_forecast_end_date,
    ranked_story_schedule.id AS forecast_cause_id,
    ranked_story_schedule.sequence_id AS forecast_cause_sequence_id,
    ranked_story_schedule.title AS forecast_cause_title,
    ranked_story_schedule.forecast_source AS forecast_cause_source
FROM visible_objective AS objective
LEFT JOIN story_stats ON story_stats.objective_id = objective.objective_id
LEFT JOIN key_result_stats ON key_result_stats.objective_id = objective.objective_id
LEFT JOIN objective_schedule ON objective_schedule.objective_id = objective.objective_id
LEFT JOIN ranked_story_schedule
    ON ranked_story_schedule.objective_id = objective.objective_id
   AND ranked_story_schedule.forecast_rank = 1
`

type GetObjectiveParams struct {
	ObjectiveID    uuid.UUID
	WorkspaceID    *uuid.UUID
	InternalAccess bool
	ActorID        uuid.UUID
}

type GetObjectiveRow struct {
	ObjectiveID             uuid.UUID
	SequenceID              int32
	Name                    string
	Description             *string
	ShortSummary            *string
	LeadUserID              *uuid.UUID
	TeamID                  *uuid.UUID
	WorkspaceID             *uuid.UUID
	StartDate               *time.Time
	EndDate                 *time.Time
	IsPrivate               bool
	CreatedAt               time.Time
	UpdatedAt               time.Time
	StatusID                *uuid.UUID
	Priority                *string
	Health                  string
	Color                   string
	CreatedBy               *uuid.UUID
	KeyResultCount          int32
	TotalStories            int32
	CancelledStories        int32
	CompletedStories        int32
	StartedStories          int32
	UnstartedStories        int32
	BacklogStories          int32
	ForecastStartDate       time.Time
	HasForecastStartDate    bool
	ForecastEndDate         time.Time
	HasForecastEndDate      bool
	ForecastCauseID         *uuid.UUID
	ForecastCauseSequenceID *int32
	ForecastCauseTitle      *string
	ForecastCauseSource     *string
}

func (q *Queries) GetObjective(ctx context.Context, arg GetObjectiveParams) (GetObjectiveRow, error) {
	row := q.db.QueryRow(ctx, getObjective,
		arg.ObjectiveID,
		arg.WorkspaceID,
		arg.InternalAccess,
		arg.ActorID,
	)
	var i GetObjectiveRow
	err := row.Scan(
		&i.ObjectiveID,
		&i.SequenceID,
		&i.Name,
		&i.Description,
		&i.ShortSummary,
		&i.LeadUserID,
		&i.TeamID,
		&i.WorkspaceID,
		&i.StartDate,
		&i.EndDate,
		&i.IsPrivate,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.StatusID,
		&i.Priority,
		&i.Health,
		&i.Color,
		&i.CreatedBy,
		&i.KeyResultCount,
		&i.TotalStories,
		&i.CancelledStories,
		&i.CompletedStories,
		&i.StartedStories,
		&i.UnstartedStories,
		&i.BacklogStories,
		&i.ForecastStartDate,
		&i.HasForecastStartDate,
		&i.ForecastEndDate,
		&i.HasForecastEndDate,
		&i.ForecastCauseID,
		&i.ForecastCauseSequenceID,
		&i.ForecastCauseTitle,
		&i.ForecastCauseSource,
	)
	return i, err
}

const listObjectives = `-- name: ListObjectives :many

WITH visible_objectives AS (
    SELECT objective.objective_id, objective.name, objective.description, objective.lead_user_id, objective.team_id, objective.workspace_id, objective.start_date, objective.end_date, objective.is_private, objective.created_at, objective.updated_at, objective.status_id, objective.priority, objective.health, objective.created_by, objective.search_vector, objective.short_summary, objective.sequence_id, objective.color
    FROM public.objectives AS objective
    INNER JOIN public.workspace_members AS membership
        ON membership.workspace_id = objective.workspace_id
       AND membership.user_id = $3
    INNER JOIN public.team_members AS team_membership
        ON team_membership.team_id = objective.team_id
       AND team_membership.user_id = $3
    INNER JOIN public.users AS actor
        ON actor.user_id = membership.user_id
       AND actor.is_active = TRUE
    WHERE objective.workspace_id = $4
	  AND membership.role IN ('member', 'admin')
      AND (
          CAST($5 AS uuid) IS NULL
          OR objective.objective_id = CAST($5 AS uuid)
      )
      AND (
          CAST($6 AS uuid) IS NULL
          OR objective.team_id = CAST($6 AS uuid)
      )
      AND (
          CAST($7 AS text) = ''
          OR objective.name ILIKE '%' || CAST($7 AS text) || '%'
      )
), story_schedule AS (
    SELECT
        story.id,
        story.sequence_id,
        story.title,
        story.objective_id,
        CASE
            WHEN MIN(block.start_at) IS NULL THEN story.start_date
            WHEN story.start_date IS NULL THEN CAST(MIN(block.start_at) AS date)
            ELSE LEAST(story.start_date, CAST(MIN(block.start_at) AS date))
        END AS forecast_start_date,
        CASE
            WHEN MAX(block.end_at) IS NULL THEN story.end_date
            WHEN story.end_date IS NULL THEN CAST(MAX(block.end_at) AS date)
            ELSE GREATEST(story.end_date, CAST(MAX(block.end_at) AS date))
        END AS forecast_end_date,
        CASE
            WHEN MAX(block.end_at) IS NOT NULL
                AND (story.end_date IS NULL OR CAST(MAX(block.end_at) AS date) >= story.end_date)
            THEN 'calendar'
            ELSE 'planning'
        END AS forecast_source
    FROM public.stories AS story
    INNER JOIN public.statuses AS status
        ON status.status_id = story.status_id
       AND status.workspace_id = story.workspace_id
    LEFT JOIN public.calendar_schedule_blocks AS block
        ON block.story_id = story.id
       AND block.workspace_id = story.workspace_id
       AND block.block_type = 'work'
    INNER JOIN visible_objectives AS objective
        ON objective.objective_id = story.objective_id
       AND objective.workspace_id = story.workspace_id
    WHERE story.deleted_at IS NULL
      AND story.archived_at IS NULL
      AND status.category NOT IN ('completed', 'cancelled')
    GROUP BY story.id, story.sequence_id, story.title, story.objective_id, story.start_date, story.end_date
), ranked_story_schedule AS (
    SELECT
        story_schedule.id, story_schedule.sequence_id, story_schedule.title, story_schedule.objective_id, story_schedule.forecast_start_date, story_schedule.forecast_end_date, story_schedule.forecast_source,
        ROW_NUMBER() OVER (
            PARTITION BY objective_id
            ORDER BY forecast_end_date DESC, sequence_id DESC, id
        ) AS forecast_rank
    FROM story_schedule
    WHERE forecast_end_date IS NOT NULL
), objective_schedule AS (
    SELECT
        objective_id,
        MIN(forecast_start_date) AS forecast_start_date,
        MAX(forecast_end_date) AS forecast_end_date
    FROM story_schedule
    GROUP BY objective_id
), story_stats AS (
    SELECT
        objective.objective_id,
        COUNT(story.id) AS total,
        COUNT(story.id) FILTER (WHERE status.category = 'cancelled') AS cancelled,
        COUNT(story.id) FILTER (WHERE status.category = 'completed') AS completed,
        COUNT(story.id) FILTER (WHERE status.category = 'started') AS started,
        COUNT(story.id) FILTER (WHERE status.category = 'unstarted') AS unstarted,
        COUNT(story.id) FILTER (WHERE status.category = 'backlog') AS backlog
    FROM visible_objectives AS objective
    LEFT JOIN public.stories AS story
        ON story.objective_id = objective.objective_id
       AND story.workspace_id = objective.workspace_id
       AND story.deleted_at IS NULL
       AND story.archived_at IS NULL
    LEFT JOIN public.statuses AS status ON status.status_id = story.status_id
       AND status.workspace_id = story.workspace_id
    GROUP BY objective.objective_id
), key_result_stats AS (
    SELECT key_result.objective_id, COUNT(*) AS total
    FROM public.key_results AS key_result
    INNER JOIN visible_objectives AS objective ON objective.objective_id = key_result.objective_id
    GROUP BY key_result.objective_id
)
SELECT
    objective.objective_id,
    objective.sequence_id,
    objective.name,
    objective.description,
    objective.short_summary,
    objective.lead_user_id,
    objective.team_id,
    objective.workspace_id,
    objective.start_date,
    objective.end_date,
    objective.is_private,
    objective.created_at,
    objective.updated_at,
    objective.status_id,
    objective.priority,
    CAST(COALESCE(CAST(objective.health AS text), '') AS text) AS health,
    objective.color,
    objective.created_by,
    CAST(COALESCE(key_result_stats.total, 0) AS integer) AS key_result_count,
    CAST(COALESCE(story_stats.total, 0) AS integer) AS total_stories,
    CAST(COALESCE(story_stats.cancelled, 0) AS integer) AS cancelled_stories,
    CAST(COALESCE(story_stats.completed, 0) AS integer) AS completed_stories,
    CAST(COALESCE(story_stats.started, 0) AS integer) AS started_stories,
    CAST(COALESCE(story_stats.unstarted, 0) AS integer) AS unstarted_stories,
    CAST(COALESCE(story_stats.backlog, 0) AS integer) AS backlog_stories,
    CAST(COALESCE(CAST(objective_schedule.forecast_start_date AS date), CAST('1970-01-01' AS date)) AS date) AS forecast_start_date,
    CAST(objective_schedule.forecast_start_date IS NOT NULL AS boolean) AS has_forecast_start_date,
    CAST(COALESCE(CAST(objective_schedule.forecast_end_date AS date), CAST('1970-01-01' AS date)) AS date) AS forecast_end_date,
    CAST(objective_schedule.forecast_end_date IS NOT NULL AS boolean) AS has_forecast_end_date,
    ranked_story_schedule.id AS forecast_cause_id,
    ranked_story_schedule.sequence_id AS forecast_cause_sequence_id,
    ranked_story_schedule.title AS forecast_cause_title,
    ranked_story_schedule.forecast_source AS forecast_cause_source
FROM visible_objectives AS objective
LEFT JOIN story_stats ON story_stats.objective_id = objective.objective_id
LEFT JOIN key_result_stats ON key_result_stats.objective_id = objective.objective_id
LEFT JOIN objective_schedule ON objective_schedule.objective_id = objective.objective_id
LEFT JOIN ranked_story_schedule
    ON ranked_story_schedule.objective_id = objective.objective_id
   AND ranked_story_schedule.forecast_rank = 1
ORDER BY objective.created_at DESC, objective.objective_id
LIMIT NULLIF(CAST($2 AS integer), 0)
OFFSET CAST($1 AS integer)
`

type ListObjectivesParams struct {
	ResultOffset int32
	ResultLimit  int32
	ActorID      uuid.UUID
	WorkspaceID  *uuid.UUID
	ObjectiveID  *uuid.UUID
	TeamID       *uuid.UUID
	Search       string
}

type ListObjectivesRow struct {
	ObjectiveID             uuid.UUID
	SequenceID              int32
	Name                    string
	Description             *string
	ShortSummary            *string
	LeadUserID              *uuid.UUID
	TeamID                  *uuid.UUID
	WorkspaceID             *uuid.UUID
	StartDate               *time.Time
	EndDate                 *time.Time
	IsPrivate               bool
	CreatedAt               time.Time
	UpdatedAt               time.Time
	StatusID                *uuid.UUID
	Priority                *string
	Health                  string
	Color                   string
	CreatedBy               *uuid.UUID
	KeyResultCount          int32
	TotalStories            int32
	CancelledStories        int32
	CompletedStories        int32
	StartedStories          int32
	UnstartedStories        int32
	BacklogStories          int32
	ForecastStartDate       time.Time
	HasForecastStartDate    bool
	ForecastEndDate         time.Time
	HasForecastEndDate      bool
	ForecastCauseID         *uuid.UUID
	ForecastCauseSequenceID *int32
	ForecastCauseTitle      *string
	ForecastCauseSource     *string
}

// Objective reads always carry both the tenant and the current actor. The
// internal flag exists only for trusted background consumers that already
// hold a tenant-scoped objective identifier.
func (q *Queries) ListObjectives(ctx context.Context, arg ListObjectivesParams) ([]ListObjectivesRow, error) {
	rows, err := q.db.Query(ctx, listObjectives,
		arg.ResultOffset,
		arg.ResultLimit,
		arg.ActorID,
		arg.WorkspaceID,
		arg.ObjectiveID,
		arg.TeamID,
		arg.Search,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListObjectivesRow{}
	for rows.Next() {
		var i ListObjectivesRow
		if err := rows.Scan(
			&i.ObjectiveID,
			&i.SequenceID,
			&i.Name,
			&i.Description,
			&i.ShortSummary,
			&i.LeadUserID,
			&i.TeamID,
			&i.WorkspaceID,
			&i.StartDate,
			&i.EndDate,
			&i.IsPrivate,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.StatusID,
			&i.Priority,
			&i.Health,
			&i.Color,
			&i.CreatedBy,
			&i.KeyResultCount,
			&i.TotalStories,
			&i.CancelledStories,
			&i.CompletedStories,
			&i.StartedStories,
			&i.UnstartedStories,
			&i.BacklogStories,
			&i.ForecastStartDate,
			&i.HasForecastStartDate,
			&i.ForecastEndDate,
			&i.HasForecastEndDate,
			&i.ForecastCauseID,
			&i.ForecastCauseSequenceID,
			&i.ForecastCauseTitle,
			&i.ForecastCauseSource,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
