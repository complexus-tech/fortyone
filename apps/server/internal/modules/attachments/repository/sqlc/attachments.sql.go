// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: attachments.sql

package attachmentssql

import (
	"context"

	"github.com/google/uuid"
)

const authorizeWorkspaceStoryAttachment = `-- name: AuthorizeWorkspaceStoryAttachment :one
SELECT attachment.attachment_id, attachment.filename, attachment.size, attachment.mime_type, attachment.uploaded_by, attachment.workspace_id, attachment.created_at, attachment.blob_name, attachment.scan_status, attachment.scan_completed_at, attachment.scan_failure_reason, attachment.optimization_status, attachment.optimization_attempts, attachment.optimization_started_at, attachment.optimization_completed_at, attachment.optimization_lease_expires_at, attachment.optimization_last_error, attachment.updated_at
FROM public.story_attachments AS relation
INNER JOIN public.stories AS story ON story.id = relation.story_id
INNER JOIN public.attachments AS attachment
    ON attachment.attachment_id = relation.attachment_id
   AND attachment.workspace_id = story.workspace_id
WHERE relation.story_id = $1
  AND relation.attachment_id = $2
  AND story.workspace_id = $3
`

type AuthorizeWorkspaceStoryAttachmentParams struct {
	StoryID      uuid.UUID
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) AuthorizeWorkspaceStoryAttachment(ctx context.Context, arg AuthorizeWorkspaceStoryAttachmentParams) (Attachment, error) {
	row := q.db.QueryRow(ctx, authorizeWorkspaceStoryAttachment, arg.StoryID, arg.AttachmentID, arg.WorkspaceID)
	var i Attachment
	err := row.Scan(
		&i.AttachmentID,
		&i.Filename,
		&i.Size,
		&i.MimeType,
		&i.UploadedBy,
		&i.WorkspaceID,
		&i.CreatedAt,
		&i.BlobName,
		&i.ScanStatus,
		&i.ScanCompletedAt,
		&i.ScanFailureReason,
		&i.OptimizationStatus,
		&i.OptimizationAttempts,
		&i.OptimizationStartedAt,
		&i.OptimizationCompletedAt,
		&i.OptimizationLeaseExpiresAt,
		&i.OptimizationLastError,
		&i.UpdatedAt,
	)
	return i, err
}

const authorizeWorkspaceStoryMedia = `-- name: AuthorizeWorkspaceStoryMedia :one
SELECT attachment.attachment_id, attachment.filename, attachment.size, attachment.mime_type, attachment.uploaded_by, attachment.workspace_id, attachment.created_at, attachment.blob_name, attachment.scan_status, attachment.scan_completed_at, attachment.scan_failure_reason, attachment.optimization_status, attachment.optimization_attempts, attachment.optimization_started_at, attachment.optimization_completed_at, attachment.optimization_lease_expires_at, attachment.optimization_last_error, attachment.updated_at
FROM public.story_inline_attachments AS media
INNER JOIN public.stories AS story ON story.id = media.story_id
INNER JOIN public.attachments AS attachment
    ON attachment.attachment_id = media.attachment_id
   AND attachment.workspace_id = story.workspace_id
WHERE media.story_id = $1
  AND media.attachment_id = $2
  AND story.workspace_id = $3
`

type AuthorizeWorkspaceStoryMediaParams struct {
	StoryID      uuid.UUID
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) AuthorizeWorkspaceStoryMedia(ctx context.Context, arg AuthorizeWorkspaceStoryMediaParams) (Attachment, error) {
	row := q.db.QueryRow(ctx, authorizeWorkspaceStoryMedia, arg.StoryID, arg.AttachmentID, arg.WorkspaceID)
	var i Attachment
	err := row.Scan(
		&i.AttachmentID,
		&i.Filename,
		&i.Size,
		&i.MimeType,
		&i.UploadedBy,
		&i.WorkspaceID,
		&i.CreatedAt,
		&i.BlobName,
		&i.ScanStatus,
		&i.ScanCompletedAt,
		&i.ScanFailureReason,
		&i.OptimizationStatus,
		&i.OptimizationAttempts,
		&i.OptimizationStartedAt,
		&i.OptimizationCompletedAt,
		&i.OptimizationLeaseExpiresAt,
		&i.OptimizationLastError,
		&i.UpdatedAt,
	)
	return i, err
}

const completeWorkspaceAttachmentOptimization = `-- name: CompleteWorkspaceAttachmentOptimization :execrows
UPDATE public.attachments
SET size = $1,
    mime_type = $2,
    optimization_status = $3,
    optimization_completed_at = CURRENT_TIMESTAMP,
    optimization_lease_expires_at = NULL,
    optimization_last_error = NULL,
    updated_at = CURRENT_TIMESTAMP
WHERE attachment_id = $4
  AND workspace_id = $5
  AND optimization_status = 'processing'
`

type CompleteWorkspaceAttachmentOptimizationParams struct {
	Size               int64
	MimeType           string
	OptimizationStatus string
	AttachmentID       uuid.UUID
	WorkspaceID        uuid.UUID
}

func (q *Queries) CompleteWorkspaceAttachmentOptimization(ctx context.Context, arg CompleteWorkspaceAttachmentOptimizationParams) (int64, error) {
	result, err := q.db.Exec(ctx, completeWorkspaceAttachmentOptimization,
		arg.Size,
		arg.MimeType,
		arg.OptimizationStatus,
		arg.AttachmentID,
		arg.WorkspaceID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const createAttachment = `-- name: CreateAttachment :one
INSERT INTO public.attachments (
    filename,
    blob_name,
    size,
    mime_type,
    uploaded_by,
    workspace_id,
    scan_status,
    optimization_status
)
VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    $8
)
RETURNING attachment_id, filename, size, mime_type, uploaded_by, workspace_id, created_at, blob_name, scan_status, scan_completed_at, scan_failure_reason, optimization_status, optimization_attempts, optimization_started_at, optimization_completed_at, optimization_lease_expires_at, optimization_last_error, updated_at
`

type CreateAttachmentParams struct {
	Filename           string
	BlobName           string
	Size               int64
	MimeType           string
	UploadedBy         *uuid.UUID
	WorkspaceID        uuid.UUID
	ScanStatus         string
	OptimizationStatus string
}

func (q *Queries) CreateAttachment(ctx context.Context, arg CreateAttachmentParams) (Attachment, error) {
	row := q.db.QueryRow(ctx, createAttachment,
		arg.Filename,
		arg.BlobName,
		arg.Size,
		arg.MimeType,
		arg.UploadedBy,
		arg.WorkspaceID,
		arg.ScanStatus,
		arg.OptimizationStatus,
	)
	var i Attachment
	err := row.Scan(
		&i.AttachmentID,
		&i.Filename,
		&i.Size,
		&i.MimeType,
		&i.UploadedBy,
		&i.WorkspaceID,
		&i.CreatedAt,
		&i.BlobName,
		&i.ScanStatus,
		&i.ScanCompletedAt,
		&i.ScanFailureReason,
		&i.OptimizationStatus,
		&i.OptimizationAttempts,
		&i.OptimizationStartedAt,
		&i.OptimizationCompletedAt,
		&i.OptimizationLeaseExpiresAt,
		&i.OptimizationLastError,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteUnreferencedWorkspaceAttachment = `-- name: DeleteUnreferencedWorkspaceAttachment :one
DELETE FROM public.attachments AS attachment
WHERE attachment.attachment_id = $1
  AND attachment.workspace_id = $2
  AND NOT EXISTS (
      SELECT 1
      FROM public.story_inline_attachments AS inline_relation
      WHERE inline_relation.attachment_id = attachment.attachment_id
  )
  AND NOT EXISTS (
      SELECT 1
      FROM public.story_attachments AS story_relation
      WHERE story_relation.attachment_id = attachment.attachment_id
  )
  AND NOT EXISTS (
      SELECT 1
      FROM public.document_attachments AS document_relation
      WHERE document_relation.attachment_id = attachment.attachment_id
  )
RETURNING attachment.attachment_id, attachment.filename, attachment.size, attachment.mime_type, attachment.uploaded_by, attachment.workspace_id, attachment.created_at, attachment.blob_name, attachment.scan_status, attachment.scan_completed_at, attachment.scan_failure_reason, attachment.optimization_status, attachment.optimization_attempts, attachment.optimization_started_at, attachment.optimization_completed_at, attachment.optimization_lease_expires_at, attachment.optimization_last_error, attachment.updated_at
`

type DeleteUnreferencedWorkspaceAttachmentParams struct {
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) DeleteUnreferencedWorkspaceAttachment(ctx context.Context, arg DeleteUnreferencedWorkspaceAttachmentParams) (Attachment, error) {
	row := q.db.QueryRow(ctx, deleteUnreferencedWorkspaceAttachment, arg.AttachmentID, arg.WorkspaceID)
	var i Attachment
	err := row.Scan(
		&i.AttachmentID,
		&i.Filename,
		&i.Size,
		&i.MimeType,
		&i.UploadedBy,
		&i.WorkspaceID,
		&i.CreatedAt,
		&i.BlobName,
		&i.ScanStatus,
		&i.ScanCompletedAt,
		&i.ScanFailureReason,
		&i.OptimizationStatus,
		&i.OptimizationAttempts,
		&i.OptimizationStartedAt,
		&i.OptimizationCompletedAt,
		&i.OptimizationLeaseExpiresAt,
		&i.OptimizationLastError,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteWorkspaceAttachment = `-- name: DeleteWorkspaceAttachment :execrows
DELETE FROM public.attachments
WHERE attachment_id = $1
  AND workspace_id = $2
`

type DeleteWorkspaceAttachmentParams struct {
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) DeleteWorkspaceAttachment(ctx context.Context, arg DeleteWorkspaceAttachmentParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteWorkspaceAttachment, arg.AttachmentID, arg.WorkspaceID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const failQueuedWorkspaceAttachmentOptimization = `-- name: FailQueuedWorkspaceAttachmentOptimization :execrows
UPDATE public.attachments
SET optimization_status = 'failed',
    optimization_completed_at = CURRENT_TIMESTAMP,
    optimization_lease_expires_at = NULL,
    optimization_last_error = $1,
    updated_at = CURRENT_TIMESTAMP
WHERE attachment_id = $2
  AND workspace_id = $3
  AND optimization_status = 'queued'
`

type FailQueuedWorkspaceAttachmentOptimizationParams struct {
	ErrorMessage *string
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) FailQueuedWorkspaceAttachmentOptimization(ctx context.Context, arg FailQueuedWorkspaceAttachmentOptimizationParams) (int64, error) {
	result, err := q.db.Exec(ctx, failQueuedWorkspaceAttachmentOptimization, arg.ErrorMessage, arg.AttachmentID, arg.WorkspaceID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const failWorkspaceAttachmentOptimization = `-- name: FailWorkspaceAttachmentOptimization :execrows
UPDATE public.attachments
SET optimization_status = 'failed',
    optimization_completed_at = CURRENT_TIMESTAMP,
    optimization_lease_expires_at = NULL,
    optimization_last_error = $1,
    updated_at = CURRENT_TIMESTAMP
WHERE attachment_id = $2
  AND workspace_id = $3
  AND optimization_status = 'processing'
`

type FailWorkspaceAttachmentOptimizationParams struct {
	ErrorMessage *string
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) FailWorkspaceAttachmentOptimization(ctx context.Context, arg FailWorkspaceAttachmentOptimizationParams) (int64, error) {
	result, err := q.db.Exec(ctx, failWorkspaceAttachmentOptimization, arg.ErrorMessage, arg.AttachmentID, arg.WorkspaceID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getWorkspaceAttachment = `-- name: GetWorkspaceAttachment :one
SELECT attachment_id, filename, size, mime_type, uploaded_by, workspace_id, created_at, blob_name, scan_status, scan_completed_at, scan_failure_reason, optimization_status, optimization_attempts, optimization_started_at, optimization_completed_at, optimization_lease_expires_at, optimization_last_error, updated_at
FROM public.attachments
WHERE attachment_id = $1
  AND workspace_id = $2
`

type GetWorkspaceAttachmentParams struct {
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) GetWorkspaceAttachment(ctx context.Context, arg GetWorkspaceAttachmentParams) (Attachment, error) {
	row := q.db.QueryRow(ctx, getWorkspaceAttachment, arg.AttachmentID, arg.WorkspaceID)
	var i Attachment
	err := row.Scan(
		&i.AttachmentID,
		&i.Filename,
		&i.Size,
		&i.MimeType,
		&i.UploadedBy,
		&i.WorkspaceID,
		&i.CreatedAt,
		&i.BlobName,
		&i.ScanStatus,
		&i.ScanCompletedAt,
		&i.ScanFailureReason,
		&i.OptimizationStatus,
		&i.OptimizationAttempts,
		&i.OptimizationStartedAt,
		&i.OptimizationCompletedAt,
		&i.OptimizationLeaseExpiresAt,
		&i.OptimizationLastError,
		&i.UpdatedAt,
	)
	return i, err
}

const linkWorkspaceStoryAttachment = `-- name: LinkWorkspaceStoryAttachment :one
INSERT INTO public.story_attachments (story_id, attachment_id)
SELECT story.id, attachment.attachment_id
FROM public.stories AS story
INNER JOIN public.attachments AS attachment
    ON attachment.attachment_id = $1
   AND attachment.workspace_id = story.workspace_id
WHERE story.id = $2
  AND story.workspace_id = $3
ON CONFLICT (story_id, attachment_id) DO UPDATE
SET attachment_id = EXCLUDED.attachment_id
RETURNING attachment_id
`

type LinkWorkspaceStoryAttachmentParams struct {
	AttachmentID uuid.UUID
	StoryID      uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) LinkWorkspaceStoryAttachment(ctx context.Context, arg LinkWorkspaceStoryAttachmentParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, linkWorkspaceStoryAttachment, arg.AttachmentID, arg.StoryID, arg.WorkspaceID)
	var attachment_id uuid.UUID
	err := row.Scan(&attachment_id)
	return attachment_id, err
}

const linkWorkspaceStoryMedia = `-- name: LinkWorkspaceStoryMedia :one
INSERT INTO public.story_inline_attachments (story_id, attachment_id, created_by)
SELECT story.id, attachment.attachment_id, $1
FROM public.stories AS story
INNER JOIN public.attachments AS attachment
    ON attachment.attachment_id = $2
   AND attachment.workspace_id = story.workspace_id
INNER JOIN public.workspace_members AS membership
    ON membership.workspace_id = story.workspace_id
   AND membership.user_id = $1
WHERE story.id = $3
  AND story.workspace_id = $4
ON CONFLICT (story_id, attachment_id) DO UPDATE
SET created_by = story_inline_attachments.created_by
RETURNING attachment_id
`

type LinkWorkspaceStoryMediaParams struct {
	CreatedBy    uuid.UUID
	AttachmentID uuid.UUID
	StoryID      uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) LinkWorkspaceStoryMedia(ctx context.Context, arg LinkWorkspaceStoryMediaParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, linkWorkspaceStoryMedia,
		arg.CreatedBy,
		arg.AttachmentID,
		arg.StoryID,
		arg.WorkspaceID,
	)
	var attachment_id uuid.UUID
	err := row.Scan(&attachment_id)
	return attachment_id, err
}

const listStoryAttachments = `-- name: ListStoryAttachments :many
SELECT attachment.attachment_id, attachment.filename, attachment.size, attachment.mime_type, attachment.uploaded_by, attachment.workspace_id, attachment.created_at, attachment.blob_name, attachment.scan_status, attachment.scan_completed_at, attachment.scan_failure_reason, attachment.optimization_status, attachment.optimization_attempts, attachment.optimization_started_at, attachment.optimization_completed_at, attachment.optimization_lease_expires_at, attachment.optimization_last_error, attachment.updated_at
FROM public.attachments AS attachment
INNER JOIN public.story_attachments AS relation
    ON relation.attachment_id = attachment.attachment_id
INNER JOIN public.stories AS story
    ON story.id = relation.story_id
WHERE relation.story_id = $1
  AND story.workspace_id = $2
  AND attachment.workspace_id = story.workspace_id
ORDER BY attachment.created_at DESC, attachment.attachment_id DESC
`

type ListStoryAttachmentsParams struct {
	StoryID     uuid.UUID
	WorkspaceID uuid.UUID
}

func (q *Queries) ListStoryAttachments(ctx context.Context, arg ListStoryAttachmentsParams) ([]Attachment, error) {
	rows, err := q.db.Query(ctx, listStoryAttachments, arg.StoryID, arg.WorkspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Attachment{}
	for rows.Next() {
		var i Attachment
		if err := rows.Scan(
			&i.AttachmentID,
			&i.Filename,
			&i.Size,
			&i.MimeType,
			&i.UploadedBy,
			&i.WorkspaceID,
			&i.CreatedAt,
			&i.BlobName,
			&i.ScanStatus,
			&i.ScanCompletedAt,
			&i.ScanFailureReason,
			&i.OptimizationStatus,
			&i.OptimizationAttempts,
			&i.OptimizationStartedAt,
			&i.OptimizationCompletedAt,
			&i.OptimizationLeaseExpiresAt,
			&i.OptimizationLastError,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const startWorkspaceAttachmentOptimization = `-- name: StartWorkspaceAttachmentOptimization :one
UPDATE public.attachments
SET optimization_status = 'processing',
    optimization_attempts = optimization_attempts + 1,
    optimization_started_at = CURRENT_TIMESTAMP,
    optimization_completed_at = NULL,
    optimization_lease_expires_at = CURRENT_TIMESTAMP + CAST($1 AS integer) * INTERVAL '1 second',
    optimization_last_error = NULL,
    updated_at = CURRENT_TIMESTAMP
WHERE attachment_id = $2
  AND workspace_id = $3
  AND (
      optimization_status IN ('queued', 'failed')
      OR (
          optimization_status = 'processing'
          AND optimization_lease_expires_at < CURRENT_TIMESTAMP
      )
  )
RETURNING attachment_id, filename, size, mime_type, uploaded_by, workspace_id, created_at, blob_name, scan_status, scan_completed_at, scan_failure_reason, optimization_status, optimization_attempts, optimization_started_at, optimization_completed_at, optimization_lease_expires_at, optimization_last_error, updated_at
`

type StartWorkspaceAttachmentOptimizationParams struct {
	LeaseSeconds int32
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) StartWorkspaceAttachmentOptimization(ctx context.Context, arg StartWorkspaceAttachmentOptimizationParams) (Attachment, error) {
	row := q.db.QueryRow(ctx, startWorkspaceAttachmentOptimization, arg.LeaseSeconds, arg.AttachmentID, arg.WorkspaceID)
	var i Attachment
	err := row.Scan(
		&i.AttachmentID,
		&i.Filename,
		&i.Size,
		&i.MimeType,
		&i.UploadedBy,
		&i.WorkspaceID,
		&i.CreatedAt,
		&i.BlobName,
		&i.ScanStatus,
		&i.ScanCompletedAt,
		&i.ScanFailureReason,
		&i.OptimizationStatus,
		&i.OptimizationAttempts,
		&i.OptimizationStartedAt,
		&i.OptimizationCompletedAt,
		&i.OptimizationLeaseExpiresAt,
		&i.OptimizationLastError,
		&i.UpdatedAt,
	)
	return i, err
}

const storyExistsInWorkspace = `-- name: StoryExistsInWorkspace :one
SELECT EXISTS (
    SELECT 1
    FROM public.stories
    WHERE id = $1
      AND workspace_id = $2
)
`

type StoryExistsInWorkspaceParams struct {
	StoryID     uuid.UUID
	WorkspaceID uuid.UUID
}

func (q *Queries) StoryExistsInWorkspace(ctx context.Context, arg StoryExistsInWorkspaceParams) (bool, error) {
	row := q.db.QueryRow(ctx, storyExistsInWorkspace, arg.StoryID, arg.WorkspaceID)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const unlinkWorkspaceStoryMedia = `-- name: UnlinkWorkspaceStoryMedia :one
DELETE FROM public.story_inline_attachments AS media
USING public.stories AS story, public.attachments AS attachment
WHERE media.story_id = story.id
  AND media.attachment_id = attachment.attachment_id
  AND media.story_id = $1
  AND media.attachment_id = $2
  AND story.workspace_id = $3
  AND attachment.workspace_id = story.workspace_id
RETURNING media.attachment_id
`

type UnlinkWorkspaceStoryMediaParams struct {
	StoryID      uuid.UUID
	AttachmentID uuid.UUID
	WorkspaceID  uuid.UUID
}

func (q *Queries) UnlinkWorkspaceStoryMedia(ctx context.Context, arg UnlinkWorkspaceStoryMediaParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, unlinkWorkspaceStoryMedia, arg.StoryID, arg.AttachmentID, arg.WorkspaceID)
	var attachment_id uuid.UUID
	err := row.Scan(&attachment_id)
	return attachment_id, err
}
