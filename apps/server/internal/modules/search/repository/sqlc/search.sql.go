// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: search.sql

package searchsql

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countSearchObjectives = `-- name: CountSearchObjectives :one
SELECT COUNT(*)
FROM objectives AS objective
INNER JOIN team_members AS team_member
    ON team_member.team_id = objective.team_id
   AND team_member.user_id = $1
INNER JOIN workspace_members AS workspace_member
    ON workspace_member.workspace_id = objective.workspace_id
   AND workspace_member.user_id = $1
INNER JOIN users AS actor
    ON actor.user_id = workspace_member.user_id
   AND actor.is_active = TRUE
   AND actor.is_system = FALSE
WHERE objective.workspace_id = CAST($2 AS uuid)
  AND (
      CAST($3 AS text) = ''
      OR objective.search_vector @@ plainto_tsquery('english', CAST($3 AS text))
      OR objective.name ILIKE '%' || CAST($3 AS text) || '%'
  )
  AND (
      CAST($4 AS uuid) IS NULL
      OR objective.team_id = CAST($4 AS uuid)
  )
  AND (
      CAST($5 AS uuid) IS NULL
      OR objective.status_id = CAST($5 AS uuid)
  )
`

type CountSearchObjectivesParams struct {
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
	QueryText   string
	TeamID      *uuid.UUID
	StatusID    *uuid.UUID
}

func (q *Queries) CountSearchObjectives(ctx context.Context, arg CountSearchObjectivesParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSearchObjectives,
		arg.ActorID,
		arg.WorkspaceID,
		arg.QueryText,
		arg.TeamID,
		arg.StatusID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const countSearchStories = `-- name: CountSearchStories :one
SELECT COUNT(*)
FROM stories AS story
INNER JOIN team_members AS team_member
    ON team_member.team_id = story.team_id
   AND team_member.user_id = $1
INNER JOIN workspace_members AS workspace_member
    ON workspace_member.workspace_id = story.workspace_id
   AND workspace_member.user_id = $1
INNER JOIN users AS actor
    ON actor.user_id = workspace_member.user_id
   AND actor.is_active = TRUE
   AND actor.is_system = FALSE
WHERE story.workspace_id = $2
  AND story.deleted_at IS NULL
  AND (
      CAST($3 AS text) = ''
      OR story.search_vector @@ plainto_tsquery('english', CAST($3 AS text))
      OR story.title ILIKE '%' || CAST($3 AS text) || '%'
      OR similarity(lower(story.title), lower(CAST($3 AS text))) >= 0.2
      OR LEAST(
          word_similarity(lower(story.title), lower(CAST($3 AS text))),
          word_similarity(lower(CAST($3 AS text)), lower(story.title))
      ) >= 0.25
  )
  AND (
      CAST($4 AS uuid) IS NULL
      OR story.team_id = CAST($4 AS uuid)
  )
  AND (
      CAST($5 AS uuid) IS NULL
      OR story.assignee_id = CAST($5 AS uuid)
  )
  AND (
      CAST($6 AS uuid) IS NULL
      OR story.status_id = CAST($6 AS uuid)
  )
  AND (
      CAST($7 AS text) IS NULL
      OR story.priority = CAST($7 AS text)
  )
  AND (
      CAST($8 AS uuid) IS NULL
      OR EXISTS (
          SELECT 1
          FROM story_labels AS filtered_label
          WHERE filtered_label.story_id = story.id
            AND filtered_label.label_id = CAST($8 AS uuid)
      )
  )
`

type CountSearchStoriesParams struct {
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
	QueryText   string
	TeamID      *uuid.UUID
	AssigneeID  *uuid.UUID
	StatusID    *uuid.UUID
	Priority    *string
	LabelID     *uuid.UUID
}

func (q *Queries) CountSearchStories(ctx context.Context, arg CountSearchStoriesParams) (int64, error) {
	row := q.db.QueryRow(ctx, countSearchStories,
		arg.ActorID,
		arg.WorkspaceID,
		arg.QueryText,
		arg.TeamID,
		arg.AssigneeID,
		arg.StatusID,
		arg.Priority,
		arg.LabelID,
	)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const findSimilarStories = `-- name: FindSimilarStories :many
WITH input AS (
    SELECT COALESCE(string_agg(token, ' ' ORDER BY token_order), '') AS normalized_title
    FROM unnest(
        regexp_split_to_array(lower(CAST($2 AS text)), '[^a-z0-9]+')
    ) WITH ORDINALITY AS tokens(token, token_order)
    WHERE token <> ''
      AND token NOT IN (
          'a', 'add', 'an', 'build', 'create', 'fix', 'for', 'implement',
          'make', 'new', 'please', 'story', 'support', 'task', 'the', 'to', 'update'
      )
), ranked AS (
    SELECT
        story.id,
        story.sequence_id,
        story.title,
        story.team_id,
        story.status_id,
        story.assignee_id,
        story.priority,
        story.updated_at,
        CAST(GREATEST(
            similarity(normalized.normalized_title, input.normalized_title),
            CASE
                WHEN normalized.normalized_title = input.normalized_title THEN 1.0
                ELSE 0.0
            END
        ) AS double precision) AS confidence
    FROM stories AS story
    INNER JOIN team_members AS team_member
        ON team_member.team_id = story.team_id
       AND team_member.user_id = $3
    INNER JOIN workspace_members AS workspace_member
        ON workspace_member.workspace_id = story.workspace_id
       AND workspace_member.user_id = $3
    INNER JOIN users AS actor
        ON actor.user_id = workspace_member.user_id
       AND actor.is_active = TRUE
       AND actor.is_system = FALSE
    CROSS JOIN input
    CROSS JOIN LATERAL (
        SELECT COALESCE(string_agg(token, ' ' ORDER BY token_order), '') AS normalized_title
        FROM unnest(
            regexp_split_to_array(lower(story.title), '[^a-z0-9]+')
        ) WITH ORDINALITY AS tokens(token, token_order)
        WHERE token <> ''
          AND token NOT IN (
              'a', 'add', 'an', 'build', 'create', 'fix', 'for', 'implement',
              'make', 'new', 'please', 'story', 'support', 'task', 'the', 'to', 'update'
          )
    ) AS normalized
    WHERE story.workspace_id = $4
      AND story.deleted_at IS NULL
      AND input.normalized_title <> ''
      AND normalized.normalized_title <> ''
      AND (
          CAST($5 AS uuid) IS NULL
          OR story.team_id = CAST($5 AS uuid)
      )
)
SELECT
    ranked.id,
    ranked.sequence_id,
    ranked.title,
    ranked.team_id,
    ranked.status_id,
    ranked.assignee_id,
    ranked.priority,
    ranked.confidence
FROM ranked
WHERE ranked.confidence >= 0.45
ORDER BY ranked.confidence DESC, ranked.updated_at DESC, ranked.title, ranked.id
LIMIT $1
`

type FindSimilarStoriesParams struct {
	ResultLimit int32
	Title       string
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
	TeamID      *uuid.UUID
}

type FindSimilarStoriesRow struct {
	ID         uuid.UUID
	SequenceID *int32
	Title      string
	TeamID     uuid.UUID
	StatusID   *uuid.UUID
	AssigneeID *uuid.UUID
	Priority   *string
	Confidence float64
}

func (q *Queries) FindSimilarStories(ctx context.Context, arg FindSimilarStoriesParams) ([]FindSimilarStoriesRow, error) {
	rows, err := q.db.Query(ctx, findSimilarStories,
		arg.ResultLimit,
		arg.Title,
		arg.ActorID,
		arg.WorkspaceID,
		arg.TeamID,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []FindSimilarStoriesRow{}
	for rows.Next() {
		var i FindSimilarStoriesRow
		if err := rows.Scan(
			&i.ID,
			&i.SequenceID,
			&i.Title,
			&i.TeamID,
			&i.StatusID,
			&i.AssigneeID,
			&i.Priority,
			&i.Confidence,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchObjectives = `-- name: SearchObjectives :many
SELECT
    objective.objective_id,
    objective.name,
    objective.description,
    objective.short_summary,
    objective.lead_user_id,
    search_lead.full_name AS lead_full_name,
    search_lead.username AS lead_username,
    objective.team_id,
    search_team.name AS team_name,
    search_team.code AS team_code,
    objective.workspace_id,
    objective.start_date,
    objective.end_date,
    objective.status_id,
    objective.priority,
    objective.health,
    objective.created_at,
    objective.updated_at,
    COUNT(*) OVER () AS total_count
FROM objectives AS objective
INNER JOIN team_members AS team_member
    ON team_member.team_id = objective.team_id
   AND team_member.user_id = $1
INNER JOIN workspace_members AS workspace_member
    ON workspace_member.workspace_id = objective.workspace_id
   AND workspace_member.user_id = $1
INNER JOIN users AS actor
    ON actor.user_id = workspace_member.user_id
   AND actor.is_active = TRUE
   AND actor.is_system = FALSE
INNER JOIN teams AS search_team
    ON search_team.team_id = objective.team_id
   AND search_team.workspace_id = objective.workspace_id
LEFT JOIN users AS search_lead
    ON search_lead.user_id = objective.lead_user_id
   AND search_lead.is_active = TRUE
   AND search_lead.is_system = FALSE
   AND EXISTS (
       SELECT 1
       FROM workspace_members AS search_lead_membership
       WHERE search_lead_membership.workspace_id = objective.workspace_id
         AND search_lead_membership.user_id = search_lead.user_id
   )
WHERE objective.workspace_id = CAST($2 AS uuid)
  AND (
      CAST($3 AS text) = ''
      OR objective.search_vector @@ plainto_tsquery('english', CAST($3 AS text))
      OR objective.name ILIKE '%' || CAST($3 AS text) || '%'
  )
  AND (
      CAST($4 AS uuid) IS NULL
      OR objective.team_id = CAST($4 AS uuid)
  )
  AND (
      CAST($5 AS uuid) IS NULL
      OR objective.status_id = CAST($5 AS uuid)
  )
ORDER BY
    CASE WHEN CAST($6 AS text) = 'updated' THEN objective.updated_at END DESC,
    CASE WHEN CAST($6 AS text) = 'created' THEN objective.created_at END DESC,
    CASE
        WHEN CAST($6 AS text) = 'relevance'
         AND CAST($3 AS text) <> ''
        THEN ts_rank(objective.search_vector, plainto_tsquery('english', CAST($3 AS text)))
    END DESC,
    objective.created_at DESC,
    objective.objective_id
LIMIT $8
OFFSET $7
`

type SearchObjectivesParams struct {
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
	QueryText   string
	TeamID      *uuid.UUID
	StatusID    *uuid.UUID
	SortBy      string
	PageOffset  int32
	PageLimit   int32
}

type SearchObjectivesRow struct {
	ObjectiveID  uuid.UUID
	Name         string
	Description  *string
	ShortSummary *string
	LeadUserID   *uuid.UUID
	LeadFullName *string
	LeadUsername *string
	TeamID       *uuid.UUID
	TeamName     string
	TeamCode     string
	WorkspaceID  *uuid.UUID
	StartDate    *time.Time
	EndDate      *time.Time
	StatusID     *uuid.UUID
	Priority     *string
	Health       *ObjectiveHealthStatus
	CreatedAt    time.Time
	UpdatedAt    time.Time
	TotalCount   int64
}

func (q *Queries) SearchObjectives(ctx context.Context, arg SearchObjectivesParams) ([]SearchObjectivesRow, error) {
	rows, err := q.db.Query(ctx, searchObjectives,
		arg.ActorID,
		arg.WorkspaceID,
		arg.QueryText,
		arg.TeamID,
		arg.StatusID,
		arg.SortBy,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SearchObjectivesRow{}
	for rows.Next() {
		var i SearchObjectivesRow
		if err := rows.Scan(
			&i.ObjectiveID,
			&i.Name,
			&i.Description,
			&i.ShortSummary,
			&i.LeadUserID,
			&i.LeadFullName,
			&i.LeadUsername,
			&i.TeamID,
			&i.TeamName,
			&i.TeamCode,
			&i.WorkspaceID,
			&i.StartDate,
			&i.EndDate,
			&i.StatusID,
			&i.Priority,
			&i.Health,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchStories = `-- name: SearchStories :many
SELECT
    story.id,
    story.sequence_id,
    story.title,
    story.parent_id,
    story.objective_id,
    story.status_id,
    search_status.name AS status_name,
    search_status.color AS status_color,
    search_status.category AS status_category,
    story.assignee_id,
    search_assignee.full_name AS assignee_full_name,
    search_assignee.username AS assignee_username,
    story.reporter_id,
    story.priority,
    story.estimate_unit,
    COALESCE(estimation.scheme, 'tshirt') AS estimate_scheme,
    story.sprint_id,
    story.key_result_id,
    story.team_id,
    search_team.name AS team_name,
    search_team.code AS team_code,
    story.workspace_id,
    story.start_date,
    story.end_date,
    story.estimated_duration_minutes,
    story.minimum_focus_block_minutes,
    story.auto_scheduling_enabled,
    story.auto_scheduling_locked,
    story.auto_scheduling_status,
    story.auto_scheduling_reason,
    story.auto_scheduling_updated_at,
    story.created_at,
    story.updated_at,
    CAST(COALESCE(
        ARRAY(
            SELECT story_label.label_id
            FROM story_labels AS story_label
            WHERE story_label.story_id = story.id
            ORDER BY story_label.label_id
        ),
        CAST(ARRAY[] AS uuid[])
    ) AS uuid[]) AS label_ids,
    COUNT(*) OVER () AS total_count
FROM stories AS story
INNER JOIN team_members AS team_member
    ON team_member.team_id = story.team_id
   AND team_member.user_id = $1
INNER JOIN workspace_members AS workspace_member
    ON workspace_member.workspace_id = story.workspace_id
   AND workspace_member.user_id = $1
INNER JOIN users AS actor
    ON actor.user_id = workspace_member.user_id
   AND actor.is_active = TRUE
   AND actor.is_system = FALSE
INNER JOIN teams AS search_team
    ON search_team.team_id = story.team_id
   AND search_team.workspace_id = story.workspace_id
LEFT JOIN statuses AS search_status
    ON search_status.status_id = story.status_id
   AND search_status.team_id = story.team_id
   AND search_status.workspace_id = story.workspace_id
LEFT JOIN users AS search_assignee
    ON search_assignee.user_id = story.assignee_id
   AND search_assignee.is_active = TRUE
   AND search_assignee.is_system = FALSE
   AND EXISTS (
       SELECT 1
       FROM workspace_members AS search_assignee_membership
       WHERE search_assignee_membership.workspace_id = story.workspace_id
         AND search_assignee_membership.user_id = search_assignee.user_id
   )
LEFT JOIN team_estimation_settings AS estimation
    ON estimation.team_id = story.team_id
WHERE story.workspace_id = $2
  AND story.deleted_at IS NULL
  AND (
      CAST($3 AS text) = ''
      OR story.search_vector @@ plainto_tsquery('english', CAST($3 AS text))
      OR story.title ILIKE '%' || CAST($3 AS text) || '%'
      OR similarity(lower(story.title), lower(CAST($3 AS text))) >= 0.2
      OR LEAST(
          word_similarity(lower(story.title), lower(CAST($3 AS text))),
          word_similarity(lower(CAST($3 AS text)), lower(story.title))
      ) >= 0.25
  )
  AND (
      CAST($4 AS uuid) IS NULL
      OR story.team_id = CAST($4 AS uuid)
  )
  AND (
      CAST($5 AS uuid) IS NULL
      OR story.assignee_id = CAST($5 AS uuid)
  )
  AND (
      CAST($6 AS uuid) IS NULL
      OR story.status_id = CAST($6 AS uuid)
  )
  AND (
      CAST($7 AS text) IS NULL
      OR story.priority = CAST($7 AS text)
  )
  AND (
      CAST($8 AS uuid) IS NULL
      OR EXISTS (
          SELECT 1
          FROM story_labels AS filtered_label
          WHERE filtered_label.story_id = story.id
            AND filtered_label.label_id = CAST($8 AS uuid)
      )
  )
ORDER BY
    CASE WHEN CAST($9 AS text) = 'updated' THEN story.updated_at END DESC,
    CASE WHEN CAST($9 AS text) = 'created' THEN story.created_at END DESC,
    CASE
        WHEN CAST($9 AS text) = 'relevance'
         AND CAST($3 AS text) <> ''
        THEN GREATEST(
            ts_rank(story.search_vector, plainto_tsquery('english', CAST($3 AS text))),
            similarity(lower(story.title), lower(CAST($3 AS text))),
            LEAST(
                word_similarity(lower(story.title), lower(CAST($3 AS text))),
                word_similarity(lower(CAST($3 AS text)), lower(story.title))
            )
        )
    END DESC,
    story.created_at DESC,
    story.id
LIMIT $11
OFFSET $10
`

type SearchStoriesParams struct {
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
	QueryText   string
	TeamID      *uuid.UUID
	AssigneeID  *uuid.UUID
	StatusID    *uuid.UUID
	Priority    *string
	LabelID     *uuid.UUID
	SortBy      string
	PageOffset  int32
	PageLimit   int32
}

type SearchStoriesRow struct {
	ID                       uuid.UUID
	SequenceID               *int32
	Title                    string
	ParentID                 *uuid.UUID
	ObjectiveID              *uuid.UUID
	StatusID                 *uuid.UUID
	StatusName               *string
	StatusColor              *string
	StatusCategory           *string
	AssigneeID               *uuid.UUID
	AssigneeFullName         *string
	AssigneeUsername         *string
	ReporterID               *uuid.UUID
	Priority                 *string
	EstimateUnit             *int16
	EstimateScheme           string
	SprintID                 *uuid.UUID
	KeyResultID              *uuid.UUID
	TeamID                   uuid.UUID
	TeamName                 string
	TeamCode                 string
	WorkspaceID              uuid.UUID
	StartDate                *time.Time
	EndDate                  *time.Time
	EstimatedDurationMinutes *int32
	MinimumFocusBlockMinutes *int32
	AutoSchedulingEnabled    bool
	AutoSchedulingLocked     bool
	AutoSchedulingStatus     string
	AutoSchedulingReason     *string
	AutoSchedulingUpdatedAt  *time.Time
	CreatedAt                time.Time
	UpdatedAt                time.Time
	LabelIds                 []uuid.UUID
	TotalCount               int64
}

func (q *Queries) SearchStories(ctx context.Context, arg SearchStoriesParams) ([]SearchStoriesRow, error) {
	rows, err := q.db.Query(ctx, searchStories,
		arg.ActorID,
		arg.WorkspaceID,
		arg.QueryText,
		arg.TeamID,
		arg.AssigneeID,
		arg.StatusID,
		arg.Priority,
		arg.LabelID,
		arg.SortBy,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SearchStoriesRow{}
	for rows.Next() {
		var i SearchStoriesRow
		if err := rows.Scan(
			&i.ID,
			&i.SequenceID,
			&i.Title,
			&i.ParentID,
			&i.ObjectiveID,
			&i.StatusID,
			&i.StatusName,
			&i.StatusColor,
			&i.StatusCategory,
			&i.AssigneeID,
			&i.AssigneeFullName,
			&i.AssigneeUsername,
			&i.ReporterID,
			&i.Priority,
			&i.EstimateUnit,
			&i.EstimateScheme,
			&i.SprintID,
			&i.KeyResultID,
			&i.TeamID,
			&i.TeamName,
			&i.TeamCode,
			&i.WorkspaceID,
			&i.StartDate,
			&i.EndDate,
			&i.EstimatedDurationMinutes,
			&i.MinimumFocusBlockMinutes,
			&i.AutoSchedulingEnabled,
			&i.AutoSchedulingLocked,
			&i.AutoSchedulingStatus,
			&i.AutoSchedulingReason,
			&i.AutoSchedulingUpdatedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.LabelIds,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
