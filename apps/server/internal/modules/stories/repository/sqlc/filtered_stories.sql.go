// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: filtered_stories.sql

package storyreadsql

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countVisibleStories = `-- name: CountVisibleStories :one
SELECT CAST(COUNT(*) AS integer)
FROM stories AS story
INNER JOIN teams AS team
    ON team.team_id = story.team_id
   AND team.workspace_id = story.workspace_id
INNER JOIN users AS actor
    ON actor.user_id = $1
   AND actor.is_active = TRUE
INNER JOIN workspace_members AS workspace_member
    ON workspace_member.workspace_id = story.workspace_id
   AND workspace_member.user_id = actor.user_id
INNER JOIN team_members AS team_member
    ON team_member.team_id = story.team_id
   AND team_member.user_id = actor.user_id
WHERE story.workspace_id = $2
  AND story.deleted_at IS NULL
  AND story.archived_at IS NULL
  AND (
      CAST($3 AS boolean)
      OR story.team_id = ANY(CAST($4 AS uuid[]))
  )
`

type CountVisibleStoriesParams struct {
	ActorID                uuid.UUID
	WorkspaceID            uuid.UUID
	UnrestrictedTeamAccess bool
	AllowedTeamIds         []uuid.UUID
}

func (q *Queries) CountVisibleStories(ctx context.Context, arg CountVisibleStoriesParams) (int32, error) {
	row := q.db.QueryRow(ctx, countVisibleStories,
		arg.ActorID,
		arg.WorkspaceID,
		arg.UnrestrictedTeamAccess,
		arg.AllowedTeamIds,
	)
	var column_1 int32
	err := row.Scan(&column_1)
	return column_1, err
}

const listVisibleFilteredStoryRows = `-- name: ListVisibleFilteredStoryRows :many

WITH read_args AS (
    SELECT
        CAST($4 AS uuid) AS actor_id,
        CAST($5 AS uuid) AS workspace_id,
        CAST($6 AS boolean) AS unrestricted_team_access,
        CAST($7 AS uuid[]) AS allowed_team_ids,
        CAST($8 AS uuid[]) AS status_ids,
        CAST($9 AS uuid[]) AS excluded_status_ids,
        CAST($10 AS uuid[]) AS assignee_ids,
        CAST($11 AS uuid[]) AS excluded_assignee_ids,
        CAST($12 AS uuid[]) AS collaborator_ids,
        CAST($13 AS uuid[]) AS reporter_ids,
        CAST($14 AS uuid[]) AS excluded_reporter_ids,
        CAST($15 AS boolean) AS has_title_contains,
        CAST($16 AS text) AS title_contains,
        CAST($17 AS boolean) AS has_title_not_contains,
        CAST($18 AS text) AS title_not_contains,
        CAST($19 AS text[]) AS priorities,
        CAST($20 AS text[]) AS excluded_priorities,
        CAST($21 AS text[]) AS categories,
        CAST($22 AS uuid[]) AS team_ids,
        CAST($23 AS uuid[]) AS excluded_team_ids,
        CAST($24 AS uuid[]) AS sprint_ids,
        CAST($25 AS uuid[]) AS excluded_sprint_ids,
        CAST($26 AS uuid[]) AS label_ids,
        CAST($27 AS uuid[]) AS excluded_label_ids,
        CAST($28 AS smallint[]) AS estimate_values,
        CAST($29 AS smallint[]) AS excluded_estimate_values,
        CAST($30 AS boolean) AS has_parent,
        CAST($31 AS uuid) AS parent_id,
        CAST($32 AS boolean) AS has_objective,
        CAST($33 AS uuid) AS objective_id,
        CAST($34 AS boolean) AS has_excluded_objective,
        CAST($35 AS uuid) AS excluded_objective_id,
        CAST($36 AS boolean) AS has_key_result,
        CAST($37 AS uuid) AS key_result_id,
        CAST($38 AS boolean) AS has_no_assignee,
        CAST($39 AS boolean) AS has_assignee,
        CAST($40 AS boolean) AS has_blocked_by,
        CAST($41 AS boolean) AS assigned_to_me,
        CAST($42 AS boolean) AS collaborating_with_me,
        CAST($43 AS boolean) AS created_by_me,
        CAST($44 AS boolean) AS show_sub_stories,
        CAST($45 AS boolean) AS has_created_after,
        CAST($46 AS timestamptz) AS created_after,
        CAST($47 AS boolean) AS has_created_before,
        CAST($48 AS timestamptz) AS created_before,
        CAST($49 AS boolean) AS has_updated_after,
        CAST($50 AS timestamptz) AS updated_after,
        CAST($51 AS boolean) AS has_updated_before,
        CAST($52 AS timestamptz) AS updated_before,
        CAST($53 AS boolean) AS has_start_date_after,
        CAST($54 AS date) AS start_date_after,
        CAST($55 AS boolean) AS has_start_date_before,
        CAST($56 AS date) AS start_date_before,
        CAST($57 AS boolean) AS has_start_date_not,
        CAST($58 AS date) AS start_date_not,
        CAST($59 AS boolean) AS has_deadline_after,
        CAST($60 AS date) AS deadline_after,
        CAST($61 AS boolean) AS has_deadline_before,
        CAST($62 AS date) AS deadline_before,
        CAST($63 AS boolean) AS has_deadline_not,
        CAST($64 AS date) AS deadline_not,
        CAST($65 AS boolean) AS has_completed_after,
        CAST($66 AS timestamptz) AS completed_after,
        CAST($67 AS boolean) AS has_completed_before,
        CAST($68 AS timestamptz) AS completed_before,
        CAST($69 AS boolean) AS is_completed,
        CAST($70 AS boolean) AS is_not_completed,
        CAST($71 AS boolean) AS include_archived,
        CAST($72 AS boolean) AS include_deleted,
        CAST($73 AS text) AS group_by,
        CAST($74 AS text) AS order_by,
        CAST($75 AS text) AS order_direction,
        CAST($76 AS boolean) AS apply_group_filter,
        CAST($77 AS text) AS group_key,
        CAST($1 AS text) AS read_mode,
        CAST($3 AS integer) AS result_limit,
        CAST($2 AS integer) AS result_offset
),
candidate_stories AS (
    SELECT
        story.id,
        story.sequence_id,
        story.title,
        CAST(COALESCE(story.priority, 'No Priority') AS text) AS priority,
        story.estimate_unit,
        CAST(COALESCE(estimation.scheme, 'tshirt') AS text) AS estimate_scheme,
        story.estimated_duration_minutes,
        story.minimum_focus_block_minutes,
        story.auto_scheduling_enabled,
        story.auto_scheduling_locked,
        story.auto_scheduling_status,
        story.auto_scheduling_reason,
        story.auto_scheduling_updated_at,
        story.parent_id,
        story.objective_id,
        objective.name AS objective_name,
        objective.description AS objective_description,
        story.sprint_id,
        sprint.name AS sprint_name,
        sprint.goal AS sprint_goal,
        sprint.start_date AS sprint_start_date,
        sprint.end_date AS sprint_end_date,
        story.team_id,
        team.code AS team_code,
        team.name AS team_name,
        story.workspace_id,
        story.status_id,
        story.assignee_id,
        CAST((
            SELECT COUNT(*)
            FROM story_collaborators AS collaborator
            WHERE collaborator.story_id = story.id
        ) AS integer) AS collaborator_count,
        story.reporter_id,
        story.key_result_id,
        story.start_date,
        story.end_date,
        story.created_at,
        story.updated_at,
        story.completed_at,
        story.deleted_at,
        story.archived_at,
        CAST(ARRAY(
            SELECT story_label.label_id
            FROM story_labels AS story_label
            WHERE story_label.story_id = story.id
            ORDER BY story_label.label_id
        ) AS uuid[]) AS label_ids,
        CAST(CASE args.group_by
            WHEN 'status' THEN COALESCE(CAST(story.status_id AS text), 'null')
            WHEN 'assignee' THEN COALESCE(CAST(story.assignee_id AS text), 'null')
            WHEN 'priority' THEN COALESCE(story.priority, 'No Priority')
            WHEN 'team' THEN CAST(story.team_id AS text)
            WHEN 'sprint' THEN COALESCE(CAST(story.sprint_id AS text), 'null')
            ELSE 'none'
        END AS text) AS group_key,
        args.order_by,
        args.order_direction,
        args.apply_group_filter,
        args.group_key AS requested_group_key,
        args.read_mode,
        args.result_limit,
        args.result_offset
    FROM stories AS story
    CROSS JOIN read_args AS args
    INNER JOIN teams AS team
        ON team.team_id = story.team_id
       AND team.workspace_id = story.workspace_id
    INNER JOIN users AS actor
        ON actor.user_id = args.actor_id
       AND actor.is_active = TRUE
    INNER JOIN workspace_members AS workspace_member
        ON workspace_member.workspace_id = story.workspace_id
       AND workspace_member.user_id = actor.user_id
    INNER JOIN team_members AS team_member
        ON team_member.team_id = story.team_id
       AND team_member.user_id = actor.user_id
    LEFT JOIN statuses AS status
        ON status.status_id = story.status_id
       AND status.team_id = story.team_id
       AND status.workspace_id = story.workspace_id
    LEFT JOIN objectives AS objective
        ON objective.objective_id = story.objective_id
       AND objective.workspace_id = story.workspace_id
    LEFT JOIN sprints AS sprint
        ON sprint.sprint_id = story.sprint_id
       AND sprint.workspace_id = story.workspace_id
    LEFT JOIN team_estimation_settings AS estimation
        ON estimation.team_id = story.team_id
       AND estimation.workspace_id = story.workspace_id
    WHERE story.workspace_id = args.workspace_id
      AND (args.unrestricted_team_access OR story.team_id = ANY(args.allowed_team_ids))
      AND (cardinality(args.status_ids) = 0 OR story.status_id = ANY(args.status_ids))
      AND (cardinality(args.excluded_status_ids) = 0 OR story.status_id IS NULL OR story.status_id <> ALL(args.excluded_status_ids))
      AND (cardinality(args.assignee_ids) = 0 OR story.assignee_id = ANY(args.assignee_ids))
      AND (cardinality(args.excluded_assignee_ids) = 0 OR story.assignee_id IS NULL OR story.assignee_id <> ALL(args.excluded_assignee_ids))
      AND (cardinality(args.collaborator_ids) = 0 OR EXISTS (
          SELECT 1
          FROM story_collaborators AS collaborator_filter
          WHERE collaborator_filter.story_id = story.id
            AND collaborator_filter.user_id = ANY(args.collaborator_ids)
      ))
      AND (cardinality(args.reporter_ids) = 0 OR story.reporter_id = ANY(args.reporter_ids))
      AND (cardinality(args.excluded_reporter_ids) = 0 OR story.reporter_id IS NULL OR story.reporter_id <> ALL(args.excluded_reporter_ids))
      AND (NOT args.has_title_contains OR (
          story.title ILIKE '%' || args.title_contains || '%'
          OR COALESCE(story.description, '') ILIKE '%' || args.title_contains || '%'
          OR COALESCE(story.description_html, '') ILIKE '%' || args.title_contains || '%'
      ))
      AND (NOT args.has_title_not_contains OR NOT (
          story.title ILIKE '%' || args.title_not_contains || '%'
          OR COALESCE(story.description, '') ILIKE '%' || args.title_not_contains || '%'
          OR COALESCE(story.description_html, '') ILIKE '%' || args.title_not_contains || '%'
      ))
      AND (cardinality(args.priorities) = 0 OR COALESCE(story.priority, 'No Priority') = ANY(args.priorities))
      AND (cardinality(args.excluded_priorities) = 0 OR COALESCE(story.priority, 'No Priority') <> ALL(args.excluded_priorities))
      AND (cardinality(args.categories) = 0 OR CAST(status.category AS text) = ANY(args.categories))
      AND (cardinality(args.team_ids) = 0 OR story.team_id = ANY(args.team_ids))
      AND (cardinality(args.excluded_team_ids) = 0 OR story.team_id <> ALL(args.excluded_team_ids))
      AND (cardinality(args.sprint_ids) = 0 OR story.sprint_id = ANY(args.sprint_ids))
      AND (cardinality(args.excluded_sprint_ids) = 0 OR story.sprint_id IS NULL OR story.sprint_id <> ALL(args.excluded_sprint_ids))
      AND (cardinality(args.label_ids) = 0 OR EXISTS (
          SELECT 1
          FROM story_labels AS included_label
          WHERE included_label.story_id = story.id
            AND included_label.label_id = ANY(args.label_ids)
      ))
      AND (cardinality(args.excluded_label_ids) = 0 OR NOT EXISTS (
          SELECT 1
          FROM story_labels AS excluded_label
          WHERE excluded_label.story_id = story.id
            AND excluded_label.label_id = ANY(args.excluded_label_ids)
      ))
      AND (cardinality(args.estimate_values) = 0 OR story.estimate_unit = ANY(args.estimate_values))
      AND (cardinality(args.excluded_estimate_values) = 0 OR story.estimate_unit IS NULL OR story.estimate_unit <> ALL(args.excluded_estimate_values))
      AND (NOT args.has_parent OR story.parent_id = args.parent_id)
      AND (args.has_parent OR args.show_sub_stories OR story.parent_id IS NULL)
      AND (NOT args.has_objective OR story.objective_id = args.objective_id)
      AND (NOT args.has_excluded_objective OR story.objective_id IS DISTINCT FROM args.excluded_objective_id)
      AND (NOT args.has_key_result OR story.key_result_id = args.key_result_id)
      AND (NOT args.has_no_assignee OR story.assignee_id IS NULL)
      AND (NOT args.has_assignee OR story.assignee_id IS NOT NULL)
      AND (NOT args.has_blocked_by OR story.blocked_by_id IS NOT NULL)
      AND (
          (NOT args.assigned_to_me AND NOT args.collaborating_with_me AND NOT args.created_by_me)
          OR (args.assigned_to_me AND story.assignee_id = args.actor_id)
          OR (args.created_by_me AND story.reporter_id = args.actor_id)
          OR (args.collaborating_with_me AND EXISTS (
              SELECT 1
              FROM story_collaborators AS actor_collaboration
              WHERE actor_collaboration.story_id = story.id
                AND actor_collaboration.user_id = args.actor_id
          ))
      )
      AND (NOT args.has_created_after OR story.created_at >= args.created_after)
      AND (NOT args.has_created_before OR story.created_at <= args.created_before)
      AND (NOT args.has_updated_after OR story.updated_at >= args.updated_after)
      AND (NOT args.has_updated_before OR story.updated_at <= args.updated_before)
      AND (NOT args.has_start_date_after OR story.start_date >= args.start_date_after)
      AND (NOT args.has_start_date_before OR story.start_date <= args.start_date_before)
      AND (NOT args.has_start_date_not OR story.start_date IS DISTINCT FROM args.start_date_not)
      AND (NOT args.has_deadline_after OR story.end_date >= args.deadline_after)
      AND (NOT args.has_deadline_before OR story.end_date <= args.deadline_before)
      AND (NOT args.has_deadline_not OR story.end_date IS DISTINCT FROM args.deadline_not)
      AND (NOT args.has_completed_after OR story.completed_at >= args.completed_after)
      AND (NOT args.has_completed_before OR story.completed_at <= args.completed_before)
      AND (NOT args.is_completed OR story.completed_at IS NOT NULL)
      AND (NOT args.is_not_completed OR story.completed_at IS NULL)
      AND ((args.include_archived AND story.archived_at IS NOT NULL) OR (NOT args.include_archived AND story.archived_at IS NULL))
      AND ((args.include_deleted AND story.deleted_at IS NOT NULL) OR (NOT args.include_deleted AND story.deleted_at IS NULL))
),
filtered_stories AS (
    SELECT id, sequence_id, title, priority, estimate_unit, estimate_scheme, estimated_duration_minutes, minimum_focus_block_minutes, auto_scheduling_enabled, auto_scheduling_locked, auto_scheduling_status, auto_scheduling_reason, auto_scheduling_updated_at, parent_id, objective_id, objective_name, objective_description, sprint_id, sprint_name, sprint_goal, sprint_start_date, sprint_end_date, team_id, team_code, team_name, workspace_id, status_id, assignee_id, collaborator_count, reporter_id, key_result_id, start_date, end_date, created_at, updated_at, completed_at, deleted_at, archived_at, label_ids, group_key, order_by, order_direction, apply_group_filter, requested_group_key, read_mode, result_limit, result_offset
    FROM candidate_stories
    WHERE NOT apply_group_filter OR group_key = requested_group_key
),
ranked_stories AS (
    SELECT
        filtered_stories.id, filtered_stories.sequence_id, filtered_stories.title, filtered_stories.priority, filtered_stories.estimate_unit, filtered_stories.estimate_scheme, filtered_stories.estimated_duration_minutes, filtered_stories.minimum_focus_block_minutes, filtered_stories.auto_scheduling_enabled, filtered_stories.auto_scheduling_locked, filtered_stories.auto_scheduling_status, filtered_stories.auto_scheduling_reason, filtered_stories.auto_scheduling_updated_at, filtered_stories.parent_id, filtered_stories.objective_id, filtered_stories.objective_name, filtered_stories.objective_description, filtered_stories.sprint_id, filtered_stories.sprint_name, filtered_stories.sprint_goal, filtered_stories.sprint_start_date, filtered_stories.sprint_end_date, filtered_stories.team_id, filtered_stories.team_code, filtered_stories.team_name, filtered_stories.workspace_id, filtered_stories.status_id, filtered_stories.assignee_id, filtered_stories.collaborator_count, filtered_stories.reporter_id, filtered_stories.key_result_id, filtered_stories.start_date, filtered_stories.end_date, filtered_stories.created_at, filtered_stories.updated_at, filtered_stories.completed_at, filtered_stories.deleted_at, filtered_stories.archived_at, filtered_stories.label_ids, filtered_stories.group_key, filtered_stories.order_by, filtered_stories.order_direction, filtered_stories.apply_group_filter, filtered_stories.requested_group_key, filtered_stories.read_mode, filtered_stories.result_limit, filtered_stories.result_offset,
        CAST(COUNT(*) OVER (PARTITION BY group_key) AS integer) AS total_count,
        ROW_NUMBER() OVER (
            PARTITION BY group_key
            ORDER BY
                CASE WHEN order_by = 'created' AND order_direction = 'asc' THEN created_at END ASC,
                CASE WHEN order_by = 'created' AND order_direction = 'desc' THEN created_at END DESC,
                CASE WHEN order_by = 'updated' AND order_direction = 'asc' THEN updated_at END ASC,
                CASE WHEN order_by = 'updated' AND order_direction = 'desc' THEN updated_at END DESC,
                CASE WHEN order_by = 'priority' AND order_direction = 'asc' THEN CASE priority
                    WHEN 'Urgent' THEN 1 WHEN 'High' THEN 2 WHEN 'Medium' THEN 3 WHEN 'Low' THEN 4 WHEN 'No Priority' THEN 5 ELSE 6 END
                END ASC,
                CASE WHEN order_by = 'priority' AND order_direction = 'desc' THEN CASE priority
                    WHEN 'Urgent' THEN 1 WHEN 'High' THEN 2 WHEN 'Medium' THEN 3 WHEN 'Low' THEN 4 WHEN 'No Priority' THEN 5 ELSE 6 END
                END DESC,
                CASE WHEN order_by = 'deadline' AND order_direction = 'asc' THEN end_date END ASC NULLS LAST,
                CASE WHEN order_by = 'deadline' AND order_direction = 'desc' THEN end_date END DESC NULLS LAST,
                CASE WHEN order_by = 'completed' AND order_direction = 'asc' THEN completed_at END ASC NULLS LAST,
                CASE WHEN order_by = 'completed' AND order_direction = 'desc' THEN completed_at END DESC NULLS LAST,
                created_at DESC,
                id ASC
        ) AS row_number
    FROM filtered_stories
)
SELECT
    id, sequence_id, title, priority, estimate_unit, estimate_scheme,
    estimated_duration_minutes, minimum_focus_block_minutes,
    auto_scheduling_enabled, auto_scheduling_locked, auto_scheduling_status,
    auto_scheduling_reason, auto_scheduling_updated_at, parent_id,
    objective_id, objective_name, objective_description, sprint_id,
    sprint_name, sprint_goal, sprint_start_date, sprint_end_date, team_id,
    team_code, team_name, workspace_id, status_id, assignee_id,
    collaborator_count, reporter_id, key_result_id, start_date, end_date,
    created_at, updated_at, completed_at, deleted_at, archived_at, label_ids,
    group_key, total_count, row_number
FROM ranked_stories
WHERE read_mode = 'page' OR row_number <= result_limit
ORDER BY
    CASE WHEN read_mode = 'grouped' THEN group_key END ASC,
    CASE WHEN read_mode = 'grouped' THEN row_number END ASC,
    CASE WHEN order_by = 'created' AND order_direction = 'asc' THEN created_at END ASC,
    CASE WHEN order_by = 'created' AND order_direction = 'desc' THEN created_at END DESC,
    CASE WHEN order_by = 'updated' AND order_direction = 'asc' THEN updated_at END ASC,
    CASE WHEN order_by = 'updated' AND order_direction = 'desc' THEN updated_at END DESC,
    CASE WHEN order_by = 'priority' AND order_direction = 'asc' THEN CASE priority
        WHEN 'Urgent' THEN 1 WHEN 'High' THEN 2 WHEN 'Medium' THEN 3 WHEN 'Low' THEN 4 WHEN 'No Priority' THEN 5 ELSE 6 END
    END ASC,
    CASE WHEN order_by = 'priority' AND order_direction = 'desc' THEN CASE priority
        WHEN 'Urgent' THEN 1 WHEN 'High' THEN 2 WHEN 'Medium' THEN 3 WHEN 'Low' THEN 4 WHEN 'No Priority' THEN 5 ELSE 6 END
    END DESC,
    CASE WHEN order_by = 'deadline' AND order_direction = 'asc' THEN end_date END ASC NULLS LAST,
    CASE WHEN order_by = 'deadline' AND order_direction = 'desc' THEN end_date END DESC NULLS LAST,
    CASE WHEN order_by = 'completed' AND order_direction = 'asc' THEN completed_at END ASC NULLS LAST,
    CASE WHEN order_by = 'completed' AND order_direction = 'desc' THEN completed_at END DESC NULLS LAST,
    created_at DESC,
    id ASC
LIMIT CASE WHEN CAST($1 AS text) = 'page' THEN CAST($3 AS integer) ELSE 2147483647 END
OFFSET CASE WHEN CAST($1 AS text) = 'page' THEN CAST($2 AS integer) ELSE 0 END
`

type ListVisibleFilteredStoryRowsParams struct {
	ReadMode               string
	ResultOffset           int32
	ResultLimit            int32
	ActorID                uuid.UUID
	WorkspaceID            uuid.UUID
	UnrestrictedTeamAccess bool
	AllowedTeamIds         []uuid.UUID
	StatusIds              []uuid.UUID
	ExcludedStatusIds      []uuid.UUID
	AssigneeIds            []uuid.UUID
	ExcludedAssigneeIds    []uuid.UUID
	CollaboratorIds        []uuid.UUID
	ReporterIds            []uuid.UUID
	ExcludedReporterIds    []uuid.UUID
	HasTitleContains       bool
	TitleContains          string
	HasTitleNotContains    bool
	TitleNotContains       string
	Priorities             []string
	ExcludedPriorities     []string
	Categories             []string
	TeamIds                []uuid.UUID
	ExcludedTeamIds        []uuid.UUID
	SprintIds              []uuid.UUID
	ExcludedSprintIds      []uuid.UUID
	LabelIds               []uuid.UUID
	ExcludedLabelIds       []uuid.UUID
	EstimateValues         []int16
	ExcludedEstimateValues []int16
	HasParent              bool
	ParentID               uuid.UUID
	HasObjective           bool
	ObjectiveID            uuid.UUID
	HasExcludedObjective   bool
	ExcludedObjectiveID    uuid.UUID
	HasKeyResult           bool
	KeyResultID            uuid.UUID
	HasNoAssignee          bool
	HasAssignee            bool
	HasBlockedBy           bool
	AssignedToMe           bool
	CollaboratingWithMe    bool
	CreatedByMe            bool
	ShowSubStories         bool
	HasCreatedAfter        bool
	CreatedAfter           time.Time
	HasCreatedBefore       bool
	CreatedBefore          time.Time
	HasUpdatedAfter        bool
	UpdatedAfter           time.Time
	HasUpdatedBefore       bool
	UpdatedBefore          time.Time
	HasStartDateAfter      bool
	StartDateAfter         time.Time
	HasStartDateBefore     bool
	StartDateBefore        time.Time
	HasStartDateNot        bool
	StartDateNot           time.Time
	HasDeadlineAfter       bool
	DeadlineAfter          time.Time
	HasDeadlineBefore      bool
	DeadlineBefore         time.Time
	HasDeadlineNot         bool
	DeadlineNot            time.Time
	HasCompletedAfter      bool
	CompletedAfter         time.Time
	HasCompletedBefore     bool
	CompletedBefore        time.Time
	IsCompleted            bool
	IsNotCompleted         bool
	IncludeArchived        bool
	IncludeDeleted         bool
	GroupBy                string
	OrderBy                string
	OrderDirection         string
	ApplyGroupFilter       bool
	GroupKey               string
}

type ListVisibleFilteredStoryRowsRow struct {
	ID                       uuid.UUID
	SequenceID               *int32
	Title                    string
	Priority                 string
	EstimateUnit             *int16
	EstimateScheme           string
	EstimatedDurationMinutes *int32
	MinimumFocusBlockMinutes *int32
	AutoSchedulingEnabled    bool
	AutoSchedulingLocked     bool
	AutoSchedulingStatus     string
	AutoSchedulingReason     *string
	AutoSchedulingUpdatedAt  *time.Time
	ParentID                 *uuid.UUID
	ObjectiveID              *uuid.UUID
	ObjectiveName            *string
	ObjectiveDescription     *string
	SprintID                 *uuid.UUID
	SprintName               *string
	SprintGoal               *string
	SprintStartDate          *time.Time
	SprintEndDate            *time.Time
	TeamID                   uuid.UUID
	TeamCode                 string
	TeamName                 string
	WorkspaceID              uuid.UUID
	StatusID                 *uuid.UUID
	AssigneeID               *uuid.UUID
	CollaboratorCount        int32
	ReporterID               *uuid.UUID
	KeyResultID              *uuid.UUID
	StartDate                *time.Time
	EndDate                  *time.Time
	CreatedAt                time.Time
	UpdatedAt                time.Time
	CompletedAt              *time.Time
	DeletedAt                *time.Time
	ArchivedAt               *time.Time
	LabelIds                 []uuid.UUID
	GroupKey                 string
	TotalCount               int32
	RowNumber                int64
}

// Static, actor-scoped filter surface used by ordinary lists, grouped lists,
// and group pagination. Boolean flags distinguish an omitted scalar filter
// from a zero value; arrays are empty when their filter is omitted.
func (q *Queries) ListVisibleFilteredStoryRows(ctx context.Context, arg ListVisibleFilteredStoryRowsParams) ([]ListVisibleFilteredStoryRowsRow, error) {
	rows, err := q.db.Query(ctx, listVisibleFilteredStoryRows,
		arg.ReadMode,
		arg.ResultOffset,
		arg.ResultLimit,
		arg.ActorID,
		arg.WorkspaceID,
		arg.UnrestrictedTeamAccess,
		arg.AllowedTeamIds,
		arg.StatusIds,
		arg.ExcludedStatusIds,
		arg.AssigneeIds,
		arg.ExcludedAssigneeIds,
		arg.CollaboratorIds,
		arg.ReporterIds,
		arg.ExcludedReporterIds,
		arg.HasTitleContains,
		arg.TitleContains,
		arg.HasTitleNotContains,
		arg.TitleNotContains,
		arg.Priorities,
		arg.ExcludedPriorities,
		arg.Categories,
		arg.TeamIds,
		arg.ExcludedTeamIds,
		arg.SprintIds,
		arg.ExcludedSprintIds,
		arg.LabelIds,
		arg.ExcludedLabelIds,
		arg.EstimateValues,
		arg.ExcludedEstimateValues,
		arg.HasParent,
		arg.ParentID,
		arg.HasObjective,
		arg.ObjectiveID,
		arg.HasExcludedObjective,
		arg.ExcludedObjectiveID,
		arg.HasKeyResult,
		arg.KeyResultID,
		arg.HasNoAssignee,
		arg.HasAssignee,
		arg.HasBlockedBy,
		arg.AssignedToMe,
		arg.CollaboratingWithMe,
		arg.CreatedByMe,
		arg.ShowSubStories,
		arg.HasCreatedAfter,
		arg.CreatedAfter,
		arg.HasCreatedBefore,
		arg.CreatedBefore,
		arg.HasUpdatedAfter,
		arg.UpdatedAfter,
		arg.HasUpdatedBefore,
		arg.UpdatedBefore,
		arg.HasStartDateAfter,
		arg.StartDateAfter,
		arg.HasStartDateBefore,
		arg.StartDateBefore,
		arg.HasStartDateNot,
		arg.StartDateNot,
		arg.HasDeadlineAfter,
		arg.DeadlineAfter,
		arg.HasDeadlineBefore,
		arg.DeadlineBefore,
		arg.HasDeadlineNot,
		arg.DeadlineNot,
		arg.HasCompletedAfter,
		arg.CompletedAfter,
		arg.HasCompletedBefore,
		arg.CompletedBefore,
		arg.IsCompleted,
		arg.IsNotCompleted,
		arg.IncludeArchived,
		arg.IncludeDeleted,
		arg.GroupBy,
		arg.OrderBy,
		arg.OrderDirection,
		arg.ApplyGroupFilter,
		arg.GroupKey,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListVisibleFilteredStoryRowsRow{}
	for rows.Next() {
		var i ListVisibleFilteredStoryRowsRow
		if err := rows.Scan(
			&i.ID,
			&i.SequenceID,
			&i.Title,
			&i.Priority,
			&i.EstimateUnit,
			&i.EstimateScheme,
			&i.EstimatedDurationMinutes,
			&i.MinimumFocusBlockMinutes,
			&i.AutoSchedulingEnabled,
			&i.AutoSchedulingLocked,
			&i.AutoSchedulingStatus,
			&i.AutoSchedulingReason,
			&i.AutoSchedulingUpdatedAt,
			&i.ParentID,
			&i.ObjectiveID,
			&i.ObjectiveName,
			&i.ObjectiveDescription,
			&i.SprintID,
			&i.SprintName,
			&i.SprintGoal,
			&i.SprintStartDate,
			&i.SprintEndDate,
			&i.TeamID,
			&i.TeamCode,
			&i.TeamName,
			&i.WorkspaceID,
			&i.StatusID,
			&i.AssigneeID,
			&i.CollaboratorCount,
			&i.ReporterID,
			&i.KeyResultID,
			&i.StartDate,
			&i.EndDate,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.CompletedAt,
			&i.DeletedAt,
			&i.ArchivedAt,
			&i.LabelIds,
			&i.GroupKey,
			&i.TotalCount,
			&i.RowNumber,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listVisibleStoryGroupCatalog = `-- name: ListVisibleStoryGroupCatalog :many
WITH catalog_args AS (
    SELECT
        CAST($2 AS uuid) AS actor_id,
        CAST($3 AS uuid) AS workspace_id,
        CAST($4 AS boolean) AS unrestricted_team_access,
        CAST($5 AS uuid[]) AS allowed_team_ids,
        CAST($6 AS uuid[]) AS team_ids,
        CAST($7 AS uuid[]) AS excluded_team_ids,
        CAST($8 AS uuid[]) AS assignee_ids,
        CAST($9 AS text) AS group_by
),
visible_teams AS (
    SELECT team.team_id, team.name
    FROM teams AS team
    CROSS JOIN catalog_args AS args
    INNER JOIN users AS actor
        ON actor.user_id = args.actor_id
       AND actor.is_active = TRUE
    INNER JOIN workspace_members AS workspace_member
        ON workspace_member.workspace_id = team.workspace_id
       AND workspace_member.user_id = actor.user_id
    INNER JOIN team_members AS team_member
        ON team_member.team_id = team.team_id
       AND team_member.user_id = actor.user_id
    WHERE team.workspace_id = args.workspace_id
      AND (args.unrestricted_team_access OR team.team_id = ANY(args.allowed_team_ids))
      AND (cardinality(args.team_ids) = 0 OR team.team_id = ANY(args.team_ids))
      AND (cardinality(args.excluded_team_ids) = 0 OR team.team_id <> ALL(args.excluded_team_ids))
),
catalog AS (
    SELECT CAST(status.status_id AS text) AS group_key, CAST(COALESCE(status.order_index, 2147483647) AS bigint) AS sort_rank, CAST(status.name AS text) AS sort_name
    FROM statuses AS status
    INNER JOIN visible_teams AS team ON team.team_id = status.team_id
    CROSS JOIN catalog_args AS args
    WHERE args.group_by = 'status' AND status.workspace_id = args.workspace_id

    UNION ALL

    SELECT CAST(team.team_id AS text), CAST(0 AS bigint), CAST(team.name AS text)
    FROM visible_teams AS team
    CROSS JOIN catalog_args AS args
    WHERE args.group_by = 'team'

    UNION ALL

    SELECT priority.group_key, priority.sort_rank, priority.group_key
    FROM (VALUES
        (CAST('Urgent' AS text), CAST(1 AS bigint)), (CAST('High' AS text), CAST(2 AS bigint)),
        (CAST('Medium' AS text), CAST(3 AS bigint)), (CAST('Low' AS text), CAST(4 AS bigint)),
        (CAST('No Priority' AS text), CAST(5 AS bigint))
    ) AS priority(group_key, sort_rank)
    CROSS JOIN catalog_args AS args
    WHERE args.group_by = 'priority'

    UNION ALL

    SELECT DISTINCT CAST(account.user_id AS text), CAST(0 AS bigint), CAST(account.username AS text)
    FROM users AS account
    INNER JOIN team_members AS membership ON membership.user_id = account.user_id
    INNER JOIN visible_teams AS team ON team.team_id = membership.team_id
    INNER JOIN workspace_members AS account_workspace_member
        ON account_workspace_member.user_id = account.user_id
    CROSS JOIN catalog_args AS args
    WHERE args.group_by = 'assignee'
      AND account_workspace_member.workspace_id = args.workspace_id
      AND account.is_active = TRUE
      AND account.is_system = FALSE
      AND (cardinality(args.assignee_ids) = 0 OR account.user_id = ANY(args.assignee_ids))

    UNION ALL

    SELECT CAST('null' AS text), CAST(1 AS bigint), CAST('Unassigned' AS text)
    FROM catalog_args AS args
    WHERE args.group_by = 'assignee' AND cardinality(args.assignee_ids) = 0

    UNION ALL

    SELECT CAST(sprint.sprint_id AS text), CAST(0 AS bigint), CAST(sprint.name AS text)
    FROM sprints AS sprint
    INNER JOIN visible_teams AS team ON team.team_id = sprint.team_id
    CROSS JOIN catalog_args AS args
    WHERE args.group_by = 'sprint' AND sprint.workspace_id = args.workspace_id

    UNION ALL

    SELECT CAST('null' AS text), CAST(1 AS bigint), CAST('No Sprint' AS text)
    FROM catalog_args AS args
    WHERE args.group_by = 'sprint'

    UNION ALL

    SELECT CAST('none' AS text), CAST(0 AS bigint), CAST('none' AS text)
    FROM catalog_args AS args
    WHERE args.group_by = 'none'
)
SELECT group_key
FROM catalog
GROUP BY group_key, sort_rank, sort_name
ORDER BY sort_rank, sort_name, group_key
LIMIT CAST($1 AS integer)
`

type ListVisibleStoryGroupCatalogParams struct {
	CatalogLimit           int32
	ActorID                uuid.UUID
	WorkspaceID            uuid.UUID
	UnrestrictedTeamAccess bool
	AllowedTeamIds         []uuid.UUID
	TeamIds                []uuid.UUID
	ExcludedTeamIds        []uuid.UUID
	AssigneeIds            []uuid.UUID
	GroupBy                string
}

// One look-ahead row lets the adapter reject an oversized catalog without
// loading an unbounded number of group identifiers into memory.
func (q *Queries) ListVisibleStoryGroupCatalog(ctx context.Context, arg ListVisibleStoryGroupCatalogParams) ([]string, error) {
	rows, err := q.db.Query(ctx, listVisibleStoryGroupCatalog,
		arg.CatalogLimit,
		arg.ActorID,
		arg.WorkspaceID,
		arg.UnrestrictedTeamAccess,
		arg.AllowedTeamIds,
		arg.TeamIds,
		arg.ExcludedTeamIds,
		arg.AssigneeIds,
		arg.GroupBy,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var group_key string
		if err := rows.Scan(&group_key); err != nil {
			return nil, err
		}
		items = append(items, group_key)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
