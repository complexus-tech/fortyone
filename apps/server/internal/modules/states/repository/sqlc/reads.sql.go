// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: reads.sql

package statessql

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const getState = `-- name: GetState :one
SELECT
    status.status_id,
    status.name,
    status.category,
    status.order_index,
    status.team_id,
    status.workspace_id,
    status.is_default,
    status.color,
    status.created_at,
    status.updated_at
FROM public.statuses AS status
WHERE status.status_id = $1
  AND status.workspace_id = $2
`

type GetStateParams struct {
	StatusID    uuid.UUID
	WorkspaceID uuid.UUID
}

type GetStateRow struct {
	StatusID    uuid.UUID
	Name        string
	Category    *string
	OrderIndex  *int32
	TeamID      uuid.UUID
	WorkspaceID uuid.UUID
	IsDefault   bool
	Color       *string
	CreatedAt   time.Time
	UpdatedAt   time.Time
}

func (q *Queries) GetState(ctx context.Context, arg GetStateParams) (GetStateRow, error) {
	row := q.db.QueryRow(ctx, getState, arg.StatusID, arg.WorkspaceID)
	var i GetStateRow
	err := row.Scan(
		&i.StatusID,
		&i.Name,
		&i.Category,
		&i.OrderIndex,
		&i.TeamID,
		&i.WorkspaceID,
		&i.IsDefault,
		&i.Color,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listStatesForMember = `-- name: ListStatesForMember :many
SELECT
    status.status_id,
    status.name,
    status.category,
    status.order_index,
    status.team_id,
    status.workspace_id,
    status.is_default,
    status.color,
    status.created_at,
    status.updated_at
FROM public.statuses AS status
INNER JOIN public.team_members AS team_membership
    ON team_membership.team_id = status.team_id
   AND team_membership.user_id = $1
INNER JOIN public.workspace_members AS workspace_membership
    ON workspace_membership.workspace_id = status.workspace_id
   AND workspace_membership.user_id = team_membership.user_id
INNER JOIN public.users AS actor
    ON actor.user_id = workspace_membership.user_id
   AND actor.is_active = TRUE
WHERE status.workspace_id = $2
ORDER BY status.order_index ASC, status.status_id ASC
`

type ListStatesForMemberParams struct {
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
}

type ListStatesForMemberRow struct {
	StatusID    uuid.UUID
	Name        string
	Category    *string
	OrderIndex  *int32
	TeamID      uuid.UUID
	WorkspaceID uuid.UUID
	IsDefault   bool
	Color       *string
	CreatedAt   time.Time
	UpdatedAt   time.Time
}

func (q *Queries) ListStatesForMember(ctx context.Context, arg ListStatesForMemberParams) ([]ListStatesForMemberRow, error) {
	rows, err := q.db.Query(ctx, listStatesForMember, arg.ActorID, arg.WorkspaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStatesForMemberRow{}
	for rows.Next() {
		var i ListStatesForMemberRow
		if err := rows.Scan(
			&i.StatusID,
			&i.Name,
			&i.Category,
			&i.OrderIndex,
			&i.TeamID,
			&i.WorkspaceID,
			&i.IsDefault,
			&i.Color,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamStates = `-- name: ListTeamStates :many
SELECT
    status.status_id,
    status.name,
    status.category,
    status.order_index,
    status.team_id,
    status.workspace_id,
    status.is_default,
    status.color,
    status.created_at,
    status.updated_at
FROM public.statuses AS status
WHERE status.workspace_id = $1
  AND status.team_id = $2
ORDER BY status.order_index ASC, status.status_id ASC
`

type ListTeamStatesParams struct {
	WorkspaceID uuid.UUID
	TeamID      uuid.UUID
}

type ListTeamStatesRow struct {
	StatusID    uuid.UUID
	Name        string
	Category    *string
	OrderIndex  *int32
	TeamID      uuid.UUID
	WorkspaceID uuid.UUID
	IsDefault   bool
	Color       *string
	CreatedAt   time.Time
	UpdatedAt   time.Time
}

func (q *Queries) ListTeamStates(ctx context.Context, arg ListTeamStatesParams) ([]ListTeamStatesRow, error) {
	rows, err := q.db.Query(ctx, listTeamStates, arg.WorkspaceID, arg.TeamID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTeamStatesRow{}
	for rows.Next() {
		var i ListTeamStatesRow
		if err := rows.Scan(
			&i.StatusID,
			&i.Name,
			&i.Category,
			&i.OrderIndex,
			&i.TeamID,
			&i.WorkspaceID,
			&i.IsDefault,
			&i.Color,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamStatesForMember = `-- name: ListTeamStatesForMember :many
SELECT
    status.status_id,
    status.name,
    status.category,
    status.order_index,
    status.team_id,
    status.workspace_id,
    status.is_default,
    status.color,
    status.created_at,
    status.updated_at
FROM public.statuses AS status
INNER JOIN public.team_members AS team_membership
    ON team_membership.team_id = status.team_id
   AND team_membership.user_id = $1
INNER JOIN public.workspace_members AS workspace_membership
    ON workspace_membership.workspace_id = status.workspace_id
   AND workspace_membership.user_id = team_membership.user_id
INNER JOIN public.users AS actor
    ON actor.user_id = workspace_membership.user_id
   AND actor.is_active = TRUE
WHERE status.workspace_id = $2
  AND status.team_id = $3
ORDER BY status.order_index ASC, status.status_id ASC
`

type ListTeamStatesForMemberParams struct {
	ActorID     uuid.UUID
	WorkspaceID uuid.UUID
	TeamID      uuid.UUID
}

type ListTeamStatesForMemberRow struct {
	StatusID    uuid.UUID
	Name        string
	Category    *string
	OrderIndex  *int32
	TeamID      uuid.UUID
	WorkspaceID uuid.UUID
	IsDefault   bool
	Color       *string
	CreatedAt   time.Time
	UpdatedAt   time.Time
}

func (q *Queries) ListTeamStatesForMember(ctx context.Context, arg ListTeamStatesForMemberParams) ([]ListTeamStatesForMemberRow, error) {
	rows, err := q.db.Query(ctx, listTeamStatesForMember, arg.ActorID, arg.WorkspaceID, arg.TeamID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTeamStatesForMemberRow{}
	for rows.Next() {
		var i ListTeamStatesForMemberRow
		if err := rows.Scan(
			&i.StatusID,
			&i.Name,
			&i.Category,
			&i.OrderIndex,
			&i.TeamID,
			&i.WorkspaceID,
			&i.IsDefault,
			&i.Color,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
